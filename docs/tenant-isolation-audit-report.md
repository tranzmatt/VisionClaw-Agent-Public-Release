# Tenant-Isolation Audit Report

_Generated 2026-06-23T07:27:19.635Z by `scripts/tenant-isolation-audit.ts` using `gemini-2.5-flash`._

**READ-ONLY audit** — findings below are surfaced for human review. No code was changed.

- Coverage: **402/402** file(s) audited · chunks 49/49 OK (0 failed)
- Findings: **331** (CRITICAL 76 · HIGH 214 · MEDIUM 39 · LOW 2)

### CRITICAL (76)

- **server/agentic/resume-worker.ts:66** — SELECT query reads tenant-scoped tables (agent_runs / agent_approvals) without constraining by tenant_id — returns rows across all tenants.
  - _Fix:_ Restrict this query to a specific tenant (or iterate per-tenant). Add a WHERE clause such as `WHERE r.tenant_id = ${tenantId}` (pass the caller/worker tenant id explicitly) or otherwise ensure the rows returned are limited to the intended tenant context before processing.
- **server/agentic/wake-scheduler.ts:79** — UPDATE ... WHERE id IN (SELECT id FROM agent_wake_schedules ...) reads/locks tenant-scoped rows (agent_wake_schedules) via a subquery that lacks a tenant_id filter — can claim schedules across tenants.
  - _Fix:_ Add tenant scoping to the inner SELECT (or execute claims per-tenant). Include `AND tenant_id = ${tenantId}` in the subselect or restructure to claim only rows for a single tenant at a time.
- **server/agentic/runs.ts:39** — appendStep updates agent_runs without constraining by tenant_id (.where(eq(agentRuns.id, runId)) only) — write to a tenant-scoped row without verifying tenant ownership.
  - _Fix:_ Include tenant scoping on updates. Add a tenantId parameter to appendStep and use `.where(and(eq(agentRuns.id, runId), eq(agentRuns.tenantId, tenantId)))`, or otherwise validate the run belongs to the caller's tenant before updating.
- **server/agentic/runs.ts:51** — updateRunState updates agent_runs with no tenant_id constraint in the WHERE clause — allows cross-tenant modification of run state.
  - _Fix:_ Require and use the tenantId when updating run state: `.where(and(eq(agentRuns.id, runId), eq(agentRuns.tenantId, tenantId)))`. Alternatively validate tenant ownership before performing the update.
- **server/agentic/runs.ts:55** — replaceRunState updates agent_runs without a tenant_id constraint in the WHERE clause — missing tenant scoping on a write to a tenant-scoped table.
  - _Fix:_ Require tenantId and include it in the WHERE clause (e.g. `.where(and(eq(agentRuns.id, runId), eq(agentRuns.tenantId, tenantId)))`) to ensure the update only affects the caller's tenant.
- **server/agentic/runs.ts:64** — completeRun updates agent_runs (status/result/completedAt) using only runId in the WHERE clause — no tenant_id check on a tenant-scoped table.
  - _Fix:_ Add tenant scoping to the update. Accept the tenantId (or derive/validate ownership) and use `.where(and(eq(agentRuns.id, runId), eq(agentRuns.tenantId, tenantId)))` to prevent cross-tenant writes.
- **server/agentic/runs.ts:73** — failRun updates agent_runs without a tenant_id constraint in the WHERE clause — risk of marking another tenant's run failed.
  - _Fix:_ Require tenantId and add it to the WHERE clause: `.where(and(eq(agentRuns.id, runId), eq(agentRuns.tenantId, tenantId)))`. Ensure all callers pass/validate tenant ownership before calling.
- **server/agentic/runs.ts:81** — pauseRun updates agent_runs with no tenant_id filter in the WHERE clause — write to tenant-scoped table lacks tenant scoping.
  - _Fix:_ Include tenant scoping on the update (e.g. `.where(and(eq(agentRuns.id, runId), eq(agentRuns.tenantId, tenantId)))`) and require tenantId as a parameter to guarantee only the caller's tenant row is modified.
- **server/auth.ts:1118** — Unbounded SELECT across auth_sessions (SELECT token, tenant_id, is_admin, expires_at FROM auth_sessions) loading all sessions from DB with no tenant filter. This returns data for all tenants.
  - _Fix:_ Avoid scanning tenant-scoped session table globally. If global scan is required for an admin operation, wrap it in a clear platform-admin check and/or restrict columns returned and audit access; otherwise add a WHERE tenant_id = ${tenantId} filter.
- **server/auth.ts:1110** — DELETE FROM auth_sessions with no WHERE (global wipe) executed in clearAllSessions — removes sessions for every tenant.
  - _Fix:_ Restrict this functionality to an explicit platform-admin operation with robust authorization, and/or add tenant scoping (DELETE ... WHERE tenant_id = ${tenantId}) to prevent accidental cross-tenant data destruction.
- **server/auto-memorize.ts:159** — runAutoMemorize pulls messages across all tenants (SELECT ... FROM messages m LEFT JOIN conversations c ... WHERE m.created_at > ${windowStart} ... LIMIT 400) without tenant_id filtering. The code comments indicate this intentionally scans ALL tenants and attributes cost to admin, but it reads tenant-scoped message content across tenants.
  - _Fix:_ If scanning all tenants is required, explicitly gate the operation to a platform-admin/service account, clearly document the global access, and ensure the LLM/processing uses only scrubbed/authorized data. Otherwise, add WHERE c.tenant_id = ${tenantId} (or m.tenant_id) to scope per-tenant.
- **server/business-tools.ts:84** — INSERT into invoice_items does not include tenant_id. If invoice_items is a tenant-scoped table this violates the tenant-isolation invariant (rows could be orphaned or ambiguous across tenants).
  - _Fix:_ Include tenant_id in the INSERT (e.g. INSERT INTO invoice_items (tenant_id, invoice_id, ...) VALUES (${tid}, ${invoiceId}, ...)) or otherwise enforce tenant ownership (transactional check/foreign key) so every invoice_item row is explicitly scoped to the caller's tenant.
- **server/business-tools.ts:334** — listContracts performs LEFT JOIN customers cust ON cust.id = c.customer_id but does not constrain the joined customers by tenant_id. Since foreign-key/tenant enforcement is not guaranteed in the DB, this can leak customer data from other tenants when IDs collide or were mis-associated.
  - _Fix:_ Restrict the join to the same tenant (e.g. LEFT JOIN customers cust ON cust.id = c.customer_id AND cust.tenant_id = c.tenant_id) or include AND cust.tenant_id = ${tid} in the JOIN/WHERE so any customer fields returned are guaranteed to belong to the caller's tenant.
- **server/ceo-orchestrator.ts:814** — Select from personas table without constraining by tenant. `const cachedPersonas = await db.select().from(personasTable);` loads personas for all tenants into memory, breaking tenant isolation and enabling cross-tenant data leakage.
  - _Fix:_ Only read personas scoped to the plan owner: filter by tenantId. E.g. use a parameterized filter (Drizzle): await db.select().from(personasTable).where(eq(personasTable.tenant_id, plan.tenantId)) (or the equivalent column name). Cache per-tenant results only. Validate any personaId/ persona row used belongs to the tenant before trusting it.
- **server/channel-routing.ts:30** — SELECT joins channel_routes -> personas with no tenant constraint. listChannelRoutes() returns routes and persona names across all tenants (no tenant_id filter), leaking tenant-specific routing and persona data.
  - _Fix:_ Make channel_routes tenant-scoped (add tenant_id column) and require a tenantId parameter on listChannelRoutes. Add WHERE cr.tenant_id = ${tenantId} AND p.tenant_id = ${tenantId} (or use Drizzle .where(eq(table.tenantId, tenantId))). Ensure JOINs include tenant ownership checks so only rows for the caller's tenant are returned.
- **server/channel-routing.ts:51** — DELETE from channel_routes without tenant constraint in setChannelRoute when personaId === null: `DELETE FROM channel_routes WHERE channel = ${channel}` will delete routes globally across tenants if channel_routes is tenant-scoped or later made so.
  - _Fix:_ Require a tenantId parameter and include it in the DELETE: DELETE FROM channel_routes WHERE channel = ${channel} AND tenant_id = ${tenantId}. If channel_routes is intended to be per-tenant, ensure the table has tenant_id and all mutation APIs accept and validate tenantId.
- **server/channel-routing.ts:53** — INSERT / ON CONFLICT into channel_routes does not constrain by tenant. The code inserts/updates channel routing globally (no tenant_id), allowing cross-tenant modifications if channel_routes should be tenant-scoped.
  - _Fix:_ Schema: add tenant_id NOT NULL to channel_routes and enforce UNIQUE(channel, tenant_id). API: accept tenantId and include it in INSERT/ON CONFLICT and in WHERE clauses (or use Drizzle .where(eq(table.tenantId, tenantId))). Also validate that personaId belongs to the same tenant before writing.
- **server/channel-routing.ts:63** — SELECT persona_id FROM channel_routes WHERE channel = ${channel} AND enabled = true has no tenant filter. If channel_routes/personas are tenant-scoped, this returns or maps persona ids across tenants.
  - _Fix:_ Require tenantId for getPersonaForChannel and add WHERE tenant_id = ${tenantId}. Validate that the returned persona_id belongs to the caller's tenant (e.g., join personas and check p.tenant_id = tenantId).
- **server/channel-routing.ts:71** — DELETE FROM channel_routes WHERE channel = ${channel} (removeChannelRoute) lacks tenant scoping. This will remove routes across all tenants if channel_routes should be tenant-scoped.
  - _Fix:_ Require tenantId and include it in the DELETE predicate: DELETE FROM channel_routes WHERE channel = ${channel} AND tenant_id = ${tenantId}. If the table is global by design, explicitly document that and ensure it is safe to be shared; otherwise add tenant_id to the schema and migrate existing rows.
- **server/chat-engine.ts:4410** — storage.createMemoryEntry(...) inserts memory rows but the payload does not include a tenantId. Per the codebase invariant, every INSERT into tenant-scoped tables must include tenant_id explicitly — omitting it allows creation of memory entries not properly scoped to the caller's tenant.
  - _Fix:_ Include tenantId in the createMemoryEntry call (e.g. add tenantId: tenantIdOrScope) and ensure storage.createMemoryEntry enforces/uses that tenantId in its INSERT (WHERE/VALUES tenant_id = ? or equivalent Drizzle .values/eq). If createMemoryEntry cannot accept tenantId, perform an explicit ownership/tenant check before inserting. Add unit tests to validate that createMemoryEntry always records tenant_id.
- **server/chat-engine.ts:4417** — storage.updateMemoryEmbedding(entry.id, emb) is called without any tenant context. This likely issues an UPDATE against a tenant-scoped memory/embeddings table without constraining tenant_id, enabling cross-tenant updates if an attacker can control entry.id.
  - _Fix:_ Change updateMemoryEmbedding to accept tenantId (or require both entryId and tenantId) and ensure the underlying UPDATE uses WHERE id = ? AND tenant_id = ?. Alternatively, look up the memory entry by id and validate its tenant matches the caller before updating. Audit and update all updateMemoryEmbedding call sites to pass tenantId.
- **server/data-protection.ts:73** — UPDATE on conversations omits tenant constraint — UPDATE conversations SET deleted_at = NOW(), deleted_by = ${deletedBy} WHERE id = ${conversationId} will update a conversation across tenants if id collisions occur.
  - _Fix:_ Include the tenant predicate in the UPDATE (e.g. WHERE id = ${conversationId} AND tenant_id = ${tenantId}) or perform this write inside a tenant-scoped transaction (withTenantTx) to ensure only the caller's tenant rows are affected.
- **server/data-protection.ts:112** — SELECT expired soft-deleted conversations scans conversations across all tenants (no tenant_id filter). This enumeration is then used to delete per-conversation data — risk of cross-tenant data deletion/leak.
  - _Fix:_ Restrict the query to a tenant (e.g. WHERE tenant_id = ${tenantId} AND deleted_at IS NOT NULL AND deleted_at < ${cutoff}) or require an explicit admin-only code path that documents privileged multi-tenant purge, and ensure strict authorization before running it.
- **server/data-protection.ts:125** — DELETE FROM messages WHERE conversation_id = ${conv.id} lacks tenant constraint — messages may be deleted across tenants if ids collide or if conversation IDs were manipulated.
  - _Fix:_ Include tenant scoping in deletes (e.g. DELETE FROM messages WHERE conversation_id = ${conv.id} AND tenant_id = ${conv.tenant_id}) or run purge operations inside a tenant-aware transaction and validate conv.tenant_id before deleting related rows.
- **server/data-protection.ts:126** — DELETE FROM compaction_archives WHERE conversation_id = ${conv.id} lacks tenant constraint — deletes archives without tenant scoping.
  - _Fix:_ Add tenant predicate (AND tenant_id = ${conv.tenant_id}) when deleting compaction_archives, or use tenant-scoped transactions and explicit ownership checks before destructive operations.
- **server/data-protection.ts:127** — DELETE FROM conversations WHERE id = ${conv.id} lacks tenant constraint — final delete may remove another tenant's conversation record.
  - _Fix:_ Restrict deletion by tenant (WHERE id = ${conv.id} AND tenant_id = ${conv.tenant_id}) or ensure this purge path is explicitly authorized for cross-tenant admin operations with proper guards.
- **server/event-bus.ts:167** — UPDATE event_log executed without scoping by tenant_id (missing WHERE tenant_id = ${tenantId}). This updates a tenant-scoped table by id only, risking cross-tenant modification if IDs collide or are abused.
  - _Fix:_ Add tenant scoping to the UPDATE (e.g. WHERE id = ${eventId} AND tenant_id = ${tenantId}) so the write is constrained to the caller's tenant.
- **server/event-bus.ts:201** — UPDATE event_log executed without scoping by tenant_id (missing WHERE tenant_id = ${tenantId}). Same pattern as above for marking routed/processed results.
  - _Fix:_ Include tenant_id in the WHERE clause (WHERE id = ${eventId} AND tenant_id = ${tenantId}) to ensure tenant isolation.
- **server/felix-loop.ts:676** — UPDATE felix_loop_runs ... WHERE id = ${runId} is executed without constraining by tenant_id. Although id is unique, the update lacks an explicit tenant filter and therefore violates the pattern of app-level tenant scoping.
  - _Fix:_ Include tenant scoping in the WHERE clause (e.g. WHERE id = ${runId} AND tenant_id = ${tenantId}) on every UPDATE to tenant-scoped tables to prevent accidental cross-tenant writes.
- **server/felix-loop.ts:690** — UPDATE felix_loop_runs SET ended_at = NOW(), error = ... WHERE id = ${runId} executed without tenant_id constraint. This occurs in an error-path recovery write and can update another tenant's run if ids overlap or are misused.
  - _Fix:_ Add tenant_id to the WHERE clause (WHERE id = ${runId} AND tenant_id = ${tenantId}) and validate tenant ownership before performing recovery/update writes.
- **server/google-drive.ts:939** — INSERT INTO project_files does not set tenant_id (INSERT INTO project_files (project_id, file_name, ...) VALUES (...)). TenantId is not included despite project_files being a tenant-scoped table, so this can create rows not tied to the caller's tenant.
  - _Fix:_ Explicitly set the tenant_id on INSERT to the caller's tenant (and validate ownership beforehand). e.g. INSERT INTO project_files (tenant_id, project_id, file_name, ...) VALUES (${resolvedTenantId}, ${params.projectId}, ${params.fileName}, ...). Do not rely on project_id foreign key alone to imply tenant ownership; application-level enforcement must always supply tenant_id.
- **server/health-audit.ts:333** — UPDATE code_proposals SET status = 'archived_stale' updates by id list with NO tenant constraint — can modify proposals for other tenants.
  - _Fix:_ Restrict the UPDATE to the tenant (e.g. WHERE id = ANY(${ids}::int[]) AND tenant_id = ${tenantId}), or select ids scoped to the tenant first and only update those. If this is intended as a global admin sweep, require an explicit admin guard and audit log.
- **server/health-audit.ts:368** — UPDATE heartbeat_tasks SET enabled = false uses id list with NO tenant constraint — can disable tasks for all tenants.
  - _Fix:_ Include tenant scoping on the UPDATE (e.g. WHERE id = ANY(${ids}::int[]) AND tenant_id = ${tenantId}) or derive the ids only for the intended tenant before mutating. Require an explicit admin guard if a global action is intended.
- **server/lib/scheduled-post-runner.ts:60** — Query/CTE selecting due rows from tenant-scoped table `scheduled_posts` omits tenant_id in WHERE clause — the runner is implicitly acting across tenants without an explicit, auditable guard.
  - _Fix:_ Either scope this poll to an explicit tenant (pass tenantId into the function) or make the cross-tenant nature explicit and gated by admin/system-only authorization/audit. If you must run across tenants, document and enforce that only a trusted system context may call this code; otherwise add WHERE tenant_id = ${tenantId} or iterate tenants explicitly so each DB operation is clearly tenant-scoped.
- **server/minerva-planner.ts:355** — Compare-and-swap UPDATE on plans does not include tenant_id in the WHERE clause. This allows changing a plan's status (approve/reject/revise) by plan id across tenants.
  - _Fix:_ Include tenant scoping in the CAS, e.g. WHERE id = ${args.planId} AND tenant_id = ${expectedTenantId} AND status = 'awaiting_approval'. Require the caller to supply/validate tenantId (fail closed if missing) so the update cannot touch other tenants' rows.
- **server/oauth-subscriptions.ts:884** — SELECT queries all rows from tenant-scoped `oauth_subscriptions` (WHERE is_active = TRUE) without filtering by tenant_id. This reads subscription rows (including refresh_token/access_token) across tenants.
  - _Fix:_ Restrict reads to a single tenant (WHERE tenant_id = ${tenantId}) or run this as an explicitly privileged/multi-tenant admin job with documented, audited justification. Avoid ad-hoc cross-tenant SELECTs that expose sensitive tokens; if you must iterate all tenants, perform per-tenant scoped queries and ensure code is running under an admin-only, audited path.
- **server/plan-executor.ts:324** — Claiming a plan uses an UPDATE without a tenant_id constraint: UPDATE plans SET status = 'executing' ... WHERE id = ${planId} AND status = 'approved' RETURNING tenant_id, ... — no tenant_id WHERE clause. This can allow cross-tenant claim/modify if planId is supplied by an attacker or bug.
  - _Fix:_ Add tenant scoping to the CAS/claim operation (e.g. WHERE id = ${planId} AND tenant_id = ${expectedTenantId} AND status = 'approved') or otherwise derive tenantId from a validated context and include it in the WHERE so only the owning tenant's plan can be claimed.
- **server/plan-executor.ts:101** — appendExecutionLog updates the plans table without scoping by tenant: UPDATE plans SET execution_log = ... WHERE id = ${planId}. This allows appending logs to plans across tenants if planId can be controlled.
  - _Fix:_ Include tenant_id in the UPDATE predicate and/or accept the tenantId in appendExecutionLog signature and use WHERE id = ${planId} AND tenant_id = ${tenantId} to ensure only the owning-tenant row is modified. Use the tenant from the claimed row rather than trusting external input.
- **server/plan-executor.ts:423** — Final plan status update uses UPDATE plans SET status = ${finalStatus} ... WHERE id = ${planId} with no tenant_id constraint. This writes terminal state across tenants if planId is mis-bound.
  - _Fix:_ Include tenant scoping in the WHERE clause (e.g. WHERE id = ${planId} AND tenant_id = ${row.tenant_id}). Prefer passing the validated tenantId into the query instead of relying solely on id.
- **server/process-governor.ts:976** — UPDATE governance_rules SET last_triggered_at = NOW(), trigger_count = trigger_count + 1 WHERE id = ${rule.id} updates governance rules by id without verifying tenant ownership (no tenant_id in WHERE). This can modify another tenant's governance rules.
  - _Fix:_ Constrain the UPDATE by tenant (e.g. WHERE id = ${rule.id} AND tenant_id = ${tenantId}) or verify the rule belongs to the tenant before updating. Any mutation of governance rules must include tenant_id checks.
- **server/proposal-verifier.ts:107** — UPDATE code_proposals ... WHERE id = ${proposalId} (clearing rebased_at) mutates a tenant-scoped table by id without tenant scoping — can modify another tenant's proposal record.
  - _Fix:_ Include tenant scoping on updates (e.g. WHERE id = ${proposalId} AND tenant_id = ${tenantId}) or verify ownership before performing any UPDATEs to code_proposals.
- **server/proposal-verifier.ts:408** — UPDATE code_proposals SET code_diff = ${newCodeDiff}, validation_result = ... WHERE id = ${proposalId} writes a tenant-scoped table without a tenant_id check — risk of cross-tenant mutation.
  - _Fix:_ Constrain the UPDATE with tenant_id = ${tenantId} (or perform an ownership check) before persisting rebased diffs to a proposal.
- **server/proposal-verifier.ts:372** — UPDATE code_proposals SET status = 'rejected', verification_status = 'skipped', ... WHERE id = ${proposalId} (in attemptRebaseOnDrift) changes proposal status without verifying tenant ownership (no tenant_id in WHERE).
  - _Fix:_ Add AND tenant_id = ${tenantId} (and require tenantId param / verify ownership) so status changes only affect rows belonging to the caller's tenant.
- **server/proposal-verifier.ts:246** — persist() does UPDATE code_proposals SET verification_status = ${status}, ... WHERE id = ${proposalId} without a tenant_id condition — mutates proposal rows across tenants.
  - _Fix:_ Include tenant scoping in the WHERE clause (e.g. WHERE id = ${proposalId} AND tenant_id = ${tenantId}) and require the tenantId in persist() so verification metadata can only be written for proposals the caller owns.
- **server/recurring-messages.ts:294** — UPDATE agent_knowledge SET content = ... WHERE id = ${r.id} executes without constraining by tenant_id. This can update rows belonging to other tenants.
  - _Fix:_ Include tenant_id in the WHERE clause (e.g. WHERE id = ${r.id} AND tenant_id = ${r.tenant_id}) or otherwise verify ownership before updating. Always constrain UPDATEs on tenant-scoped tables by tenant_id.
- **server/research-engine.ts:388** — DELETE FROM agent_knowledge WHERE source = 'autoresearch' AND expires_at < NOW() executes without tenant_id constraint — this will delete autoresearch rows for all tenants.
  - _Fix:_ Constrain the DELETE to the intended tenant (WHERE tenant_id = ${tenantId} AND ...), or run per-tenant cleanup loops. Never run mass DELETEs on tenant-scoped tables without explicit tenant scoping and audit justification.
- **server/research-engine.ts:1046** — INSERT INTO model_registry_updates (update_type, model_id, model_data, status) omits tenant_id. The model_registry_updates table was created with tenant_id DEFAULT 1, so this INSERT will implicitly attribute new rows to tenant 1 (cross-tenant pollution).
  - _Fix:_ Always set tenant_id explicitly on INSERTs into tenant-scoped tables (VALUES (${session.tenantId}, ...)). Additionally, remove or avoid DEFAULT 1 for tenant_id in schema creation so INSERT omissions fail rather than defaulting to another tenant.
- **server/routes.ts:160** — Startup routine restoreUploadsFromDb runs a SELECT on file_storage without constraining by tenant (no WHERE tenant_id = ...). This reads all tenants' file blobs and writes them to the shared uploads directory, allowing cross-tenant data exposure.
  - _Fix:_ Constrain the query to a specific tenant (WHERE tenant_id = ${tid}) or iterate per-tenant and explicitly authorize the operation. If the intent is a system-wide backfill, run it under an explicit admin-only routine that documents and limits visibility, but avoid writing other tenants' files into a shared public uploads directory without tenant-scoped protections.
- **server/routes.ts:3889** — When restoring missing uploads the code does fileDb.select().from(file_storage).where(inArray(file_storage.filename, [...missingFiles])) with no tenant filter. Rows from other tenants may be returned and then written to disk / embedded for the current request, causing cross-tenant data leakage.
  - _Fix:_ Include tenant scoping in the WHERE clause (e.g. AND file_storage.tenant_id = ${tenantId}) or join against a session-scoped ownership table before restoring. Validate ownership of each returned row before writing it to the tenant's working directory or serving it to the caller.
- **server/routes.ts:4410** — INSERT into tenant-scoped table 'project_notes' does not set tenant_id. The raw SQL INSERT INTO project_notes (project_id, note, author) VALUES (...) omits tenant_id, violating the invariant that every INSERT must include tenant_id.
  - _Fix:_ Include tenant_id in the INSERT and ensure the project belongs to the caller's tenant. For example INSERT INTO project_notes (tenant_id, project_id, note, author) VALUES (${tenantId}, ${projId}, ${snapshot.slice(0,5000)}, ${'system:context-guard'}). Also validate project ownership (project_id → tenant_id) before inserting.
- **server/second-opinion.ts:402** — settleFusionReservation updates agent_cost_ledger WHERE id = ${reservationId} with no tenant_id constraint. This function writes settled cost data to the cost ledger and, without tenant scoping, can alter another tenant's ledger row (financial integrity risk).
  - _Fix:_ Require and use the tenantId when settling a reservation (e.g. UPDATE agent_cost_ledger SET ... WHERE id = ${reservationId} AND tenant_id = ${tenantId}). Either change the function signature to accept tenantId or look up and verify the row's tenant_id before updating. Use parameterized queries/Drizzle .where(eq(table.tenant_id, tenantId)).
- **server/second-opinion.ts:421** — releaseFusionReservation updates agent_cost_ledger SET cost_usd='0', operation='second_opinion:released' WHERE id = ${reservationId} without constraining tenant_id. This can zero-out reservations for other tenants' ledger rows.
  - _Fix:_ Include tenant_id in the WHERE clause (e.g. WHERE id = ${reservationId} AND tenant_id = ${tenantId}) and make the function accept/validate tenantId, or verify ownership prior to updating. Always perform ledger updates with tenant scoping to preserve per-tenant isolation.
- **server/seed.ts:2844** — Unscoped read+update on tenant-scoped table research_programs: `SELECT id, name, model FROM research_programs WHERE is_active = true` (no tenant filter) followed by `UPDATE research_programs SET model = ${defaultModel} WHERE id = ${p.id}` (update uses only id, no tenant constraint). This can read/modify rows across tenants.
  - _Fix:_ Always scope both the SELECT and the UPDATE to the caller's tenant. E.g. `SELECT ... FROM research_programs WHERE tenant_id = ${tenantId} AND is_active = true` and `UPDATE research_programs SET model = ${defaultModel} WHERE id = ${p.id} AND tenant_id = ${tenantId}` (or use Drizzle .where(eq(table.tenantId, tenantId))). Ensure you never update by id alone without verifying tenant ownership.
- **server/seed.ts:3136** — Unscoped UPDATE on tenant-scoped table projects: `UPDATE projects SET primary_conversation_id = 272 WHERE id = 15 AND primary_conversation_id IS NULL` lacks a tenant_id constraint and may modify another tenant's row.
  - _Fix:_ Include tenant scoping in the WHERE clause (e.g. `WHERE id = ${id} AND tenant_id = ${tenantId} AND primary_conversation_id IS NULL`) or validate the project's tenant before performing the update. Prefer Drizzle .where(eq(projects.tenantId, tenantId)) to enforce ownership.
- **server/seed.ts:3203** — Unscoped SELECT on tenant-scoped table conversations: `SELECT DISTINCT c.id, c.project_id FROM conversations c ...` has no tenant_id filter and may return rows across tenants.
  - _Fix:_ Add tenant scoping to the query (e.g. `WHERE c.tenant_id = ${tenantId} AND c.project_id IS NOT NULL ...`) or otherwise filter by the caller's tenant before operating on the results.
- **server/seed.ts:3212** — INSERT into project_conversations without tenant_id: `INSERT INTO project_conversations (project_id, conversation_id) VALUES (${r.project_id}, ${r.id})` does not set tenant_id (or validate ownership), violating tenant-isolation invariant and potentially linking rows across tenants.
  - _Fix:_ Include tenant_id explicitly in the INSERT (e.g. `VALUES (${tenantId}, ${r.project_id}, ${r.id})`) and validate that both project_id and conversation_id belong to that tenant before inserting. Use Drizzle .values({ tenant_id: tenantId, ... }) or equivalent and enforce ownership checks prior to writes.
- **server/self-improvement.ts:394** — UPDATE on experiments uses .where(eq(experiments.id, experiment.id)) with NO tenant constraint. This allows updating another tenant's experiment row if an id is supplied from outside the caller's tenant.
  - _Fix:_ Constrain the UPDATE to the caller's tenant as well: .where(and(eq(experiments.id, experiment.id), eq(experiments.tenantId, tenantId))). Always include tenant_id in WHERE for UPDATE/DELETE on tenant-scoped tables.
- **server/self-improvement.ts:413** — Error-path UPDATE on experiments (in catch block) uses .where(eq(experiments.id, experiment.id)) with NO tenant constraint, permitting cross-tenant modification.
  - _Fix:_ Add the tenant constraint to the WHERE clause (e.g. and(eq(experiments.id, experiment.id), eq(experiments.tenantId, tenantId))) when marking the row as error. Ensure all UPDATEs on experiments include tenant_id check.
- **server/stability-watchdog.ts:177** — SELECT from `heartbeat_logs` (recent error aggregation) is executed without any tenant_id constraint. This tenant-agnostic read can lead to cross-tenant detection and subsequent actions based on other tenants' logs.
  - _Fix:_ Scope the query by tenant_id (or iterate per-tenant): include WHERE tenant_id = ${tenantId} (or use a tenant-aware helper) so results are isolated to a single tenant. If the watchdog must run global maintenance, require explicit admin-only intent and robust safeguards to avoid cross-tenant side effects.
- **server/stability-watchdog.ts:211** — DELETE FROM `heartbeat_tasks` is executed without tenant scoping (no tenant_id filter), which will remove disabled delegation tasks across all tenants.
  - _Fix:_ Restrict deletions to a specific tenant (e.g. WHERE tenant_id = ${tenantId} AND ...) or run cleanup per-tenant. If global cleanup is intended, require an explicit admin-only code path and audit logging; otherwise fail-closed and include tenant_id in the WHERE clause.
- **server/stability-watchdog.ts:259** — DELETE FROM `heartbeat_logs` (pruning old logs) is executed without tenant scoping (no tenant_id filter). This prunes logs for all tenants indiscriminately.
  - _Fix:_ Add tenant_id filter to the DELETE (or prune per-tenant) to avoid removing other tenants' logs. Use a tenant-aware sweep or require admin-only execution if global log pruning is desired.
- **server/stability-watchdog.ts:192** — Code identifies flaky task ids by querying tenant-agnostic `heartbeat_logs`, then calls storage.getHeartbeatTasks() and storage.updateHeartbeatTask(taskId, ...) without verifying tenant ownership — resulting in disabling tasks across tenants.
  - _Fix:_ When deriving task ids from logs, also obtain and verify tenant_id (or join heartbeat_logs to heartbeat_tasks by tenant_id) before calling updateHeartbeatTask. Ensure updateHeartbeatTask is invoked with an explicit tenant context or use a tenant-guard that fails-closed if ownership cannot be proven.
- **server/storage.ts:468** — deletePersona updates/deletes tenant-scoped tables (conversations, memory_entries, daily_notes) filtering only by personaId, not tenant_id — this can affect rows across all tenants.
  - _Fix:_ Operations that touch tenant-scoped tables must include tenant scoping. Add a tenantId parameter to deletePersona (or require it from caller) and include eq(conversations.tenantId, tenantId)/eq(memory_entries.tenantId, tenantId)/eq(daily_notes.tenantId, tenantId) in each WHERE clause. If persona rows are shared, explicitly document and limit cross-tenant effects or perform per-tenant deletes only.
- **server/storage.ts:766** — archiveExpiredMemories executes a global UPDATE on `memory_entries` (SET status='archived' WHERE status='active' AND expires_at <= NOW()) with no tenant scoping — this will archive rows for all tenants simultaneously.
  - _Fix:_ Run archival per-tenant (require tenantId) or iterate tenants and apply the UPDATE scoped to each tenant. At minimum, add tenant filter to the WHERE clause and require a tenantId parameter for this operation.
- **server/storage.ts:782** — archiveStaleMemories executes a global UPDATE on `memory_entries` (SET status='archived' WHERE ... created_at <= cutoff ...) with no tenant_id constraint — affects all tenants indiscriminately.
  - _Fix:_ Same as above: require tenantId and scope the UPDATE by tenant_id, or iterate tenants and perform per-tenant archivals. Add a safe admin-only API if truly intended to run globally, and gate it heavily.
- **server/storage.ts:1213** — getAllDataForExport performs many unscoped SELECTs (conversations, messages, memory_entries, agent_knowledge, etc.) returning entire DB content across tenants.
  - _Fix:_ Restrict this API to admin-only contexts and require an explicit tenant filter for tenant-scoped tables. If an export of all tenants is intended, gate it with strong authorization and make it explicit; otherwise add tenantId arguments and apply tenantScope to each SELECT.
- **server/tool-learning.ts:283** — UPDATE on tenant-scoped table `custom_tools` sets implementation WHERE id = tool.id with NO tenant_id constraint — write can affect rows from other tenants if ids collide or attacker supplies tool object from another tenant.
  - _Fix:_ Restrict updates to the owning tenant: .where(and(eq(customTools.id, tool.id), eq(customTools.tenantId, tenantId))). Validate the tool belongs to the caller tenant before updating. Use transactions or row-level checks where appropriate.
- **server/tool-learning.ts:300** — DELETE from tenant-scoped table `custom_tools` WHERE id = tool.id with NO tenant_id constraint — destructive operation lacks tenant scoping.
  - _Fix:_ Always include tenant ownership in delete predicates: .where(and(eq(customTools.id, tool.id), eq(customTools.tenantId, tenantId))). Verify the caller's tenant owns the row before deleting.
- **server/tools.ts:7350** — UPDATE on conversations performed without constraining to the caller's tenant (no tenant_id in WHERE). This can modify another tenant's conversation (set project_id) if a foreign conversation id is supplied.
  - _Fix:_ Add tenant scoping to the UPDATE, e.g. UPDATE conversations SET project_id = ${newProject.id} WHERE id = ${params._conversationId} AND tenant_id = ${tenantId}; or call a helper that asserts the conversation belongs to the caller's tenant before performing the update.
- **server/tools.ts:18921** — UPDATE on tenant-scoped table 'memory_entries' does not include tenant_id in WHERE clause (UPDATE memory_entries SET ... WHERE id = ${id}). Relying on a previously-read id is insufficient — the WHERE must re-assert tenant ownership.
  - _Fix:_ Constrain the UPDATE by tenant. Example: include AND tenant_id = ${tid} in the WHERE clause or use Drizzle: .where(eq(memoryEntries.id, id), eq(memoryEntries.tenantId, tid)). This prevents accidental or malicious cross-tenant updates if an id is reused or guessed.
- **server/tools.ts:19094** — UPDATE on tenant-scoped table 'memory_entries' uses WHERE id = ANY(${ids}) without also enforcing tenant_id. ids were derived from a prior tenant-scoped SELECT, but the UPDATE must independently enforce tenant scoping.
  - _Fix:_ Add tenant scoping to the UPDATE: WHERE tenant_id = ${tid} AND id = ANY(${ids}) (or with Drizzle: .where(in(memoryEntries.id, ids), eq(memoryEntries.tenantId, tid))). Always include tenant_id in UPDATE/DELETE WHERE clauses.
- **server/tools.ts:19902** — UPDATE on tenant-scoped table 'memory_entries' does not include tenant_id in WHERE clause (UPDATE memory_entries SET ... WHERE id = ${id}).
  - _Fix:_ Constrain the UPDATE by tenant: add AND tenant_id = ${tid} to the WHERE clause or use Drizzle .where(eq(memoryEntries.id, id), eq(memoryEntries.tenantId, tid)). Ensure all writes to tenant-scoped tables include an explicit tenant filter.
- **server/tools.ts:19553** — db.update(proposedSkills).where(eq(proposedSkills.id, row.id)) updates proposed_skills but does not include tenant scoping. proposed_skills is inserted with tenantId earlier, so updates must ensure the row belongs to the caller's tenant.
  - _Fix:_ Include tenant constraint in the WHERE: .where(eq(proposedSkills.id, row.id), eq(proposedSkills.tenantId, tid)) or, if using raw SQL, add AND tenant_id = ${tid}. This avoids cross-tenant modification of proposed skill rows.
- **server/tools.ts:19695** — db.update(proposedSkills).where(eq(proposedSkills.id, row.id)) used to mark a proposal 'rejected' lacks tenant_id filtering.
  - _Fix:_ Add tenant scoping to the WHERE clause (e.g. .where(eq(proposedSkills.id, row.id), eq(proposedSkills.tenantId, tid))). All UPDATEs to tenant-scoped tables must include tenant_id to enforce isolation.
- **server/tools.ts:19778** — db.update(proposedSkills).where(eq(proposedSkills.id, row.id)) used to mark a proposal 'accepted' / promoted lacks tenant_id filtering.
  - _Fix:_ Include tenant constraint when updating proposed_skills (e.g. .where(eq(proposedSkills.id, row.id), eq(proposedSkills.tenantId, tid))). Also consider using transactions/checked-update semantics to avoid TOCTOU where the row ownership could change between SELECT and UPDATE.
- **server/wiring-invariants.ts:180** — checkOutcomeCanary runs a multi-part query that reads tenant-scoped tables (research_experiments, code_proposals via research_sessions, research_schedules) without constraining by tenant_id. This will aggregate/inspect data across all tenants violating the app-only tenant-isolation invariant.
  - _Fix:_ Restrict these subqueries to a specific tenant (e.g. add AND e.tenant_id = :tenantId / AND cp.tenant_id = :tenantId / AND rs.tenant_id = :tenantId / AND s.tenant_id = :tenantId) or explicitly iterate per-tenant and run the check per-tenant. If the intent is a global admin scan, make that explicit, run it under a hardened admin path, and ensure callers understand and accept cross-tenant visibility.

### HIGH (214)

- **server/ab-optimizer.ts:112** — SELECT from tenant-scoped table `experiments` is executed without constraining by tenant_id (SELECT ... FROM experiments WHERE category = 'ab_test' AND status = 'running' LIMIT 100). This reads rows across all tenants.
  - _Fix:_ Run the sweep scoped per-tenant (add WHERE tenant_id = <tenant>), or iterate tenants and run the query per-tenant. Do NOT run a global SELECT against tenant-scoped tables without an explicit tenant filter.
- **server/agent-activity.ts:122** — UPDATE on tenant-scoped table `agent_activity` is performed without scoping by tenant_id (db.update(agentActivity).set(...).where(eq(agentActivity.id, activityId))). This may update another tenant's row.
  - _Fix:_ Include the tenant constraint in the WHERE clause, e.g. .where(and(eq(agentActivity.id, activityId), eq(agentActivity.tenantId, tenantId))). Ensure callers pass tenantId and use it here.
- **server/agent-channels.ts:198** — Query joining channel_messages (cm) with channel_subscriptions (cs) does not constrain the subscriptions table by tenant_id (JOIN channel_subscriptions cs ON cs.channel_id = cm.channel_id AND cs.persona_id = ${personaId}). This can match subscriptions from other tenants.
  - _Fix:_ Restrict the join to the caller's tenant, e.g. JOIN channel_subscriptions cs ON cs.channel_id = cm.channel_id AND cs.persona_id = ${personaId} AND cs.tenant_id = ${params.tenantId}, and/or add cs.tenant_id = ${params.tenantId} in the WHERE clause.
- **server/agent-eval.ts:218** — UPDATE on tenant-scoped table `agent_evals` is done without a tenant constraint (UPDATE agent_evals SET ... WHERE id = ${evalId}). This can modify eval rows for other tenants.
  - _Fix:_ Add tenant scoping to the UPDATE, e.g. WHERE id = ${evalId} AND tenant_id = ${tenantId}. Validate tenantId at runtime before performing the update.
- **server/agentic-engines.ts:116** — SELECT from `plans` omits tenant scoping (SELECT id FROM plans WHERE source = ... AND source_ref = ...). If `plans` is tenant-scoped this reads across tenants.
  - _Fix:_ Constrain the query by tenant_id (AND tenant_id = ${insight.tenantId}), or otherwise ensure `plans` is intentionally global and safe to query cross-tenant. Prefer explicit tenant scoping for tenant-scoped tables.
- **server/agentic-engines.ts:150** — UPDATE on tenant-scoped table `ai_insights` uses only id in WHERE (WHERE id = ${insight.insightId}) and lacks tenant_id constraint. This can update rows belonging to other tenants if ids are not globally unique per-tenant.
  - _Fix:_ Include the tenant constraint in the WHERE clause, e.g. WHERE id = ${insight.insightId} AND tenant_id = ${insight.tenantId}.
- **server/agentic-engines.ts:162** — Retry sweep SELECT from tenant-scoped `ai_insights` is executed without a tenant filter (SELECT id, tenant_id, ... FROM ai_insights WHERE status='applied' AND priority='high' AND action_taken = ...). This reads rows across all tenants.
  - _Fix:_ Either restrict the sweep to a single tenant per run (add WHERE tenant_id = ${tenantId}) or otherwise ensure you explicitly and safely handle multi-tenant iteration. Do not read tenant-scoped tables globally without explicit tenant scoping.
- **server/agentic-engines.ts:246** — Bulk UPDATE on tenant-scoped table `ai_insights` is executed without a tenant constraint (UPDATE ai_insights SET status = 'applied' WHERE status = 'new' AND category IN (...)). This updates rows across all tenants.
  - _Fix:_ Run this operation per-tenant (add WHERE tenant_id = ${tenantId}) or otherwise restrict the update to the intended tenant set. Avoid global updates on tenant-scoped tables.
- **server/agentic-engines.ts:263** — UPDATE ai_insights SET action_taken = ... WHERE id = ${Number(r.id)} is executed without tenant_id in the WHERE clause — per-row update after a global sweep can clobber other tenants' rows.
  - _Fix:_ Include tenant scoping on the UPDATE, e.g. WHERE id = ${Number(r.id)} AND tenant_id = ${Number(r.tenant_id)}. Always pair id-based updates with tenant_id checks.
- **server/agentic-engines.ts:342** — SELECT against `heartbeat_logs` omits tenant restriction (SELECT COUNT... FROM heartbeat_logs WHERE task_name != 'Self-Reflection'). If heartbeat_logs is tenant-scoped this reads data across tenants.
  - _Fix:_ Add tenant filtering, e.g. WHERE tenant_id = ${tenantId} AND task_name != 'Self-Reflection', or run tenant-scoped aggregation per-tenant.
- **server/agentic-engines.ts:449** — SELECT recent heartbeat logs (SELECT task_name, status, output FROM heartbeat_logs WHERE status = 'success' ORDER BY created_at DESC LIMIT 10) lacks tenant filtering and may return other tenants' logs.
  - _Fix:_ Constrain the query to the tenant, e.g. WHERE tenant_id = ${tenantId} AND status = 'success'. Ensure all reads from tenant-scoped logs include tenant_id.
- **server/agentic-engines.ts:518** — SELECT aggregation over `heartbeat_logs` (SELECT task_name, status, COUNT(*)... FROM heartbeat_logs GROUP BY ...) is not scoped by tenant_id — this returns cross-tenant aggregates.
  - _Fix:_ Add tenant_id = ${tenantId} to the WHERE clause so you only aggregate this tenant's logs, or iterate tenants and run per-tenant aggregations.
- **server/agentic-engines.ts:531** — SELECT from `heartbeat_tasks` omits tenant restriction (SELECT name, cron_expression, enabled, last_run_at, run_all FROM research_schedules WHERE tenant_id = ${tenantId} then SELECT name, cron_expression, enabled, last_run_at FROM heartbeat_tasks WHERE enabled = true ORDER BY name). If heartbeat_tasks is tenant-scoped this reads across tenants.
  - _Fix:_ Include tenant_id = ${tenantId} in the WHERE clause for heartbeat_tasks, e.g. WHERE tenant_id = ${tenantId} AND enabled = true. Ensure all tenant-scoped tables are filtered by tenant.
- **server/agentic-features.ts:526** — UPDATE on `lead_enrichments` uses only id in the WHERE clause (UPDATE lead_enrichments SET ... WHERE id = ${lead.id}) — no tenant constraint, so the update may touch another tenant's row.
  - _Fix:_ Add tenant scoping to the UPDATE: WHERE id = ${lead.id} AND tenant_id = ${params.tenantId}. Validate tenant context before updating.
- **server/agentic-features.ts:600** — Duplicate-check SELECT against `outreach_enrollments` omits tenant constraint (SELECT id FROM outreach_enrollments WHERE sequence_id = ${params.sequenceId} AND contact_email = ${params.contactEmail} AND status = 'active'). This can cause cross-tenant collisions or incorrect duplicate detection.
  - _Fix:_ Include tenant_id = ${params.tenantId} in the WHERE clause so you only check enrollments for the caller's tenant.
- **server/agentic-features.ts:674** — UPDATE outreach_enrollments SET ... WHERE id = ${enrollment.id} is executed without tenant scoping. Although the row was selected earlier under a tenant filter, the UPDATE must also include tenant_id to avoid races/clobbering.
  - _Fix:_ Include tenant_id in the WHERE clause of the UPDATE, e.g. WHERE id = ${enrollment.id} AND tenant_id = ${params.tenantId}.
- **server/agentic-features.ts:724** — UPDATE outreach_enrollments SET reply_classification = ... WHERE id = ${row.id} is executed without tenant scoping. This can modify another tenant's enrollment row if ids collide or a race occurs.
  - _Fix:_ Constrain the UPDATE with tenant_id, e.g. WHERE id = ${row.id} AND tenant_id = ${params.tenantId}.
- **server/agentic/approvals.ts:101** — Bulk expiry update on `agent_approvals` is executed without tenant restriction (UPDATE agentApprovals SET status = 'expired' WHERE status = 'pending' AND expiresAt < now()). This updates approvals across all tenants.
  - _Fix:_ Make the expiry sweep tenant-scoped or include tenant_id in the WHERE clause. If the sweep must be global, take extra care to validate ownership before performing per-row actions (but prefer per-tenant runs to avoid cross-tenant updates).
- **server/agentic/department-budgets.ts:143** — SELECT DISTINCT tenant_id, department FROM department_budgets WHERE status = 'active' — query against tenant-scoped table without constraining by tenant_id.
  - _Fix:_ If this is intended to be per-tenant work, add an explicit tenant_id WHERE clause (e.g. WHERE tenant_id = ${tenantId}) or iterate tenants under an explicit, audited admin context. If it must scan all tenants, ensure it runs only with an authorized admin role/process and add explicit checks/auditing or restrict returned columns to avoid cross-tenant data leakage.
- **server/auth.ts:244** — SELECT from tenant-scoped table auth_sessions without constraining by tenant_id (SELECT tenant_id, is_admin, expires_at FROM auth_sessions WHERE token = ${token}). This allows cross-tenant session lookups by token.
  - _Fix:_ Constrain session reads to the caller's tenant (e.g. WHERE token = ${token} AND tenant_id = ${callerTenantId}) or otherwise ensure the caller is authorized to perform a global session lookup (platform-admin) and document/guard that explicitly.
- **server/auth.ts:238** — DELETE from auth_sessions without tenant constraint (DELETE FROM auth_sessions WHERE token = ${token}). Deleting sessions by token may affect any tenant.
  - _Fix:_ Include tenant_id in delete predicates (DELETE ... WHERE token = ${token} AND tenant_id = ${callerTenantId}) or require a platform-admin guard for global deletes.
- **server/auth.ts:1105** — DELETE FROM auth_sessions WHERE expires_at < ${now} deletes sessions across all tenants (no tenant_id constraint).
  - _Fix:_ Run per-tenant cleanup (DELETE ... WHERE tenant_id = ${tenantId} AND expires_at < ${now}) or gate this global cleanup behind a platform-admin operation and audit/limit its scope.
- **server/auth.ts:986** — SELECT from password_reset_tokens using only token hash (SELECT tenant_id, email, expires_at FROM password_reset_tokens WHERE token = ${tokenHash}) has no tenant_id constraint. This can be used to look up/reset tokens across tenants.
  - _Fix:_ Include tenant_id in the WHERE clause where appropriate (WHERE token = ${tokenHash} AND tenant_id = ${expectedTenantId}), or require that the caller prove tenant ownership before performing the lookup. If the token lookup is intentionally global, restrict to a platform-admin flow and audit it.
- **server/auth.ts:996** — DELETE FROM password_reset_tokens WHERE token = ${tokenHash} deletes reset tokens without tenant constraint.
  - _Fix:_ Delete tokens with both token and tenant_id (DELETE ... WHERE token = ${tokenHash} AND tenant_id = ${tenantId}) or otherwise ensure the operation is limited to an authorized/administrative context.
- **server/auth.ts:1004** — DELETE FROM password_reset_tokens WHERE expires_at < ${Date.now()} deletes tokens for all tenants with no tenant filter.
  - _Fix:_ Perform per-tenant expiry cleanup (DELETE ... WHERE tenant_id = ${tenantId} AND expires_at < ${now}) or gate global expiry maintenance behind an admin-only job with strict controls.
- **server/auto-memorize.ts:100** — isDuplicate() queries memory_entries across all tenants (SELECT fact FROM memory_entries WHERE created_at > NOW() - INTERVAL '60 days' ORDER BY id DESC LIMIT 200) with no tenant filter — dedupe is performed globally.
  - _Fix:_ Limit dedup checks to the relevant tenant (WHERE tenant_id = ${tenantId} ...) or otherwise separate cross-tenant indexing from per-tenant dedup logic. Ensure enqueue/insert uses the correct tenant context.
- **server/auto-asset-capture.ts:184** — Backfill SELECT scans conversations across all tenants (SELECT DISTINCT c.id, c.project_id, c.tenant_id FROM conversations c WHERE c.project_id IS NOT NULL AND ...) with no tenant constraint.
  - _Fix:_ Restrict backfill scans to a specific tenant (WHERE c.tenant_id = ${tenantId}) or run the backfill as an explicitly authorized admin job with clear controls and auditing. Alternatively paginate per-tenant.
- **server/auto-asset-capture.ts:207** — Backfill reads messages for each conversation without constraining messages by tenant (SELECT content FROM messages WHERE conversation_id = ${c.id} ...). The initial conversation scan was unscoped, so messages may be read across tenants.
  - _Fix:_ When backfilling, ensure each conversation is validated as belonging to a tenant before reading messages; add WHERE m.conversation_id = ${c.id} AND c.tenant_id = ${tenantId} or perform per-tenant backfills.
- **server/auto-transcript.ts:128** — Backfill SELECT scans conversations across all tenants (SELECT DISTINCT c.id, c.project_id, c.tenant_id FROM conversations c WHERE c.project_id IS NOT NULL AND (SELECT COUNT(*) FROM messages WHERE conversation_id = c.id) > 0) with no tenant filter.
  - _Fix:_ Run backfill per-tenant (add WHERE c.tenant_id = ${tenantId}) or require this job to be an explicitly authorized admin operation with tight controls and auditing.
- **server/auto-transcript.ts:171** — missingLinks SELECT queries conversations with c.project_id IS NOT NULL and NOT EXISTS(...) without a tenant_id filter (SELECT c.id, c.project_id, c.tenant_id FROM conversations c WHERE c.project_id IS NOT NULL AND NOT EXISTS (...)). This can iterate and update across tenants.
  - _Fix:_ Constrain missingLinks detection per-tenant (WHERE c.tenant_id = ${tenantId}) or run as an admin-only job with explicit authorization and auditing to avoid cross-tenant modifications.
- **server/business-tools.ts:75** — INSERT INTO invoices writes customer_id from caller params without validating that the referenced customer belongs to the same tenant. This can create cross-tenant references (data integrity/isolation risk) because there is no DB-enforced FK/tenant check.
  - _Fix:_ Before inserting, verify the supplied customer_id is owned by the tenant (SELECT id FROM customers WHERE id = ${params.customer_id} AND tenant_id = ${tid}) and reject or null-out the value if not found. Alternatively, enforce the invariant by only allowing customer_id values that are proven to belong to tid.
- **server/business-tools.ts:321** — createContract INSERT sets customer_id from caller input without verifying the customer belongs to the caller's tenant. Writing a foreign id into a tenant-scoped relationship without ownership validation can link across tenants under the app-level-only isolation model.
  - _Fix:_ Validate the provided customer_id is owned by the tenant before creating the contract (e.g. SELECT 1 FROM customers WHERE id = ${params.customer_id} AND tenant_id = ${tid} and fail if absent), or omit the customer_id unless ownership is confirmed. Consider adding a DB-side guard or transaction to enforce consistency.
- **server/chat-engine.ts:4151** — Call to storage.attachCitations(...) is made without any tenant context/tenantId. If attachCitations performs DB writes against tenant-scoped tables it must be constrained to the caller's tenant; currently this call passes only a message id and could allow cross-tenant attachment or a write that isn't filtered by tenant_id.
  - _Fix:_ Pass the tenant id explicitly (and/or the tenant-scoped message row) to storage.attachCitations, or first verify that the message id belongs to the current tenant before calling attachCitations. Prefer changing the storage API to accept a tenantId (or enforce ownership lookup) and ensure every internal SQL for attachCitations uses WHERE tenant_id = <tenantId> or equivalent Drizzle filtering.
- **server/commitments.ts:135** — SELECT from tenant-scoped table `commitments` performed without a tenant_id filter (reads all tenants).
  - _Fix:_ If this is intended as an admin/owner-only operation, enforce explicit admin authorization and document the intent. Otherwise, restrict the query by tenant (e.g. add WHERE tenant_id = ${tenantId}) or iterate per-tenant and include tenant_id in the WHERE clause. Avoid blind multi-tenant reads to prevent cross-tenant data exposure.
- **server/credential-vault.ts:169** — UPDATE on `credential_vault` (setting last_used_at) uses WHERE id = ${id} with no tenant_id constraint — allows updating another tenant's row if IDs collide.
  - _Fix:_ Include tenant ownership in the WHERE clause, e.g. UPDATE credential_vault SET last_used_at = NOW() WHERE id = ${id} AND tenant_id = ${tenantId}. Alternatively, perform the update using the already-fetched credential row (which was tenant-validated) or fail-closed if tenantId is not provided.
- **server/credential-vault.ts:176** — UPDATE on `credential_vault` (setting last_used_at) uses WHERE id = ${cred.id} with no tenant_id constraint — can touch other tenants' rows.
  - _Fix:_ Constrain the UPDATE by tenant: UPDATE credential_vault SET last_used_at = NOW() WHERE id = ${cred.id} AND tenant_id = ${tenantId}. Ensure tenantId is validated and passed through to the function.
- **server/compaction.ts:463** — INSERT into tenant-scoped table `memory_entries` sets tenant_id to ${tenantId} but does not validate that tenantId is present/valid (the function accepts tenantId?: number). This can result in NULL or incorrect tenant values being inserted.
  - _Fix:_ Require and validate a non-empty integer tenantId before inserting (fail-closed if missing). If called from contexts where tenantId may be absent, skip/abort the insert or explicitly pass the calling tenant. Ensure all INSERTs into tenant-scoped tables set a validated tenant_id.
- **server/data-protection.ts:170** — Selecting messages for a conversation in backupConversationToDrive uses WHERE conversation_id = ${conversationId} but omits tenant_id = ${tenantId}. This reads rows from messages without explicit tenant scoping.
  - _Fix:_ Add tenant scoping to message reads (e.g. WHERE conversation_id = ${conversationId} AND tenant_id = ${tenantId}) to enforce application-level isolation, or use withTenantTx to bind tenant context for the duration of the operation.
- **server/data-protection.ts:176** — Selecting compaction_archives for a conversation in backupConversationToDrive uses WHERE conversation_id = ${conversationId} but does not constrain by tenant_id, allowing cross-tenant reads.
  - _Fix:_ Add AND tenant_id = ${tenantId} to the SELECT on compaction_archives, or validate ownership earlier so later selects include the tenant predicate.
- **server/data-protection.ts:145** — verifyMessageSaved executes SELECT id FROM messages WHERE conversation_id = ${conversationId} AND role = ${role} but does not accept or use a tenantId — read lacks tenant filter and can return results across tenants.
  - _Fix:_ Require tenantId as a parameter and include AND tenant_id = ${tenantId} in the WHERE clause, or run the check inside a tenant-scoped transaction to guarantee isolation.
- **server/crews-engine.ts:874** — addFlowStep inserts step.crew_id = ${params.crewId || null} without verifying that the provided crewId belongs to the caller's tenant. Writing a foreign tenant's id into a tenant-scoped flow_steps row can lead to cross-tenant linking or confusion.
  - _Fix:_ When a caller supplies a foreign id (crewId) validate ownership first (e.g., fetch the crew with getCrew(params.crewId, params.tenantId) or assert the crew's tenant_id matches params.tenantId) before inserting it into flow_steps, or disallow cross-tenant references entirely.
- **server/delivery-pipeline.ts:184** — updateDeliveryLog updates delivery_logs using only delivery id (WHERE eq(delivery_logs.id, id)) with NO tenant_id constraint — a write against a tenant-scoped table without scoping.
  - _Fix:_ Require/accept a tenantId argument and include it in the WHERE (e.g. .where(and(eq(deliveryLogs.id, id), eq(deliveryLogs.tenantId, tenantId)))) or otherwise verify ownership before performing the update. All callers must pass the caller's tenantId or use an internal-trusted path that explicitly documents the global privilege.
- **server/delivery-pipeline.ts:672** — SELECT on delivery_logs for idempotency (where stripePaymentId = ...) has NO tenant constraint — reads a tenant-scoped table globally, which can leak another tenant's delivery info or allow cross-tenant interactions.
  - _Fix:_ Scope the lookup by tenant (e.g. add AND tenant_id = ${req.tenantId}) or require the caller to provide/validate tenant context. If stripePaymentId is intended to be globally unique and safe to query globally, document that assumption and ensure the index/uniqueness and access controls are appropriate; otherwise filter by tenant_id.
- **server/delivery-pipeline.ts:709** — UPDATE delivery_logs (clearing stripePaymentId / appending metadata) uses WHERE id = prior.id with NO tenant filter — write to a tenant-scoped table without verifying tenant ownership.
  - _Fix:_ Include tenant scoping in the WHERE (AND tenant_id = <owner>) or perform the update via a transactional path that has already validated the tenant and passes tenantId explicitly to the update function.
- **server/delivery-pipeline.ts:1002** — getDeliveryByStripePayment queries delivery_logs by stripePaymentId with NO tenant constraint — returns tenant-scoped rows across tenants.
  - _Fix:_ Add an explicit tenant parameter and filter by tenant_id, or make clear and safe the intention to search globally (and document/guard it). Avoid returning tenant data without scoping to the caller's tenant.
- **server/doc-collections.ts:294** — UPDATE doc_chunks SET embedding = ... WHERE id = ${row.id} performed without tenant_id constraint — writing to a tenant-scoped table by id only risks touching another tenant's row if ids are guessed/leaked.
  - _Fix:_ Include tenant_id in the WHERE clause (AND tenant_id = ${tenantId}) or perform the update via a transaction/SELECT that returns and verifies tenant ownership before updating. Propagate tenantId into this update path.
- **server/embeddings.ts:712** — SELECT FROM memory_entries (SELECT id, embedding FROM memory_entries WHERE embedding IS NOT NULL AND embedding_vec IS NULL AND status = 'active' LIMIT 200) executes with NO tenant filter — enumerates tenant-scoped rows across tenants.
  - _Fix:_ Run backfill operations scoped per-tenant (require a tenantId argument) or include tenant_id filtering in the query. Administrative/global maintenance jobs that must operate across tenants should be auditable and run with explicit admin privileges; do not use unscoped queries in general runtime code.
- **server/embeddings.ts:722** — UPDATE memory_entries SET embedding_vec = ... WHERE id = ${row.id} has NO tenant_id constraint — updates tenant-scoped rows by id without verifying tenant ownership.
  - _Fix:_ Add tenant_id to the WHERE clause (AND tenant_id = ${tenantId}) or ensure the backfill loop carries tenant context for each id and uses it in the UPDATE to prevent cross-tenant writes.
- **server/embeddings.ts:728** — SELECT FROM agent_knowledge (SELECT id, embedding FROM agent_knowledge WHERE embedding IS NOT NULL AND embedding_vec IS NULL ...) performs no tenant filtering — reads tenant-scoped rows across tenants.
  - _Fix:_ Scope the selection per-tenant (require tenantId) or otherwise ensure this maintenance task runs in an auditable admin context. Avoid unscoped selects on tenant-scoped tables in runtime code.
- **server/embeddings.ts:739** — UPDATE agent_knowledge SET embedding_vec = ... WHERE id = ${row.id} executed without tenant_id constraint — writes to a tenant-scoped table by id only.
  - _Fix:_ Include tenant_id in the WHERE clause (AND tenant_id = ${tenantId}) or perform the update in a context that verifies ownership for each row before updating.
- **server/embeddings.ts:680** — storeEmbeddingVec builds and executes an UPDATE via sql.raw(...) without including tenant scoping (UPDATE <table> SET embedding_vec=..., embedding=... WHERE id = ${id}) — a generic helper updating tenant-scoped tables with id only.
  - _Fix:_ Add a tenantId parameter to storeEmbeddingVec and include AND tenant_id = ${tenantId} in the WHERE. Also keep the runtime allowlist for table names, and avoid updating rows by id alone when the table is tenant-scoped.
- **server/felix-brain.ts:19** — SELECT FROM agent_runs performs a global read with no tenant_id filter. agent_runs appears tenant-scoped but the query returns rows for all tenants, which can leak cross-tenant state into in-memory taskStateStore.
  - _Fix:_ Restrict the query by tenant (e.g. WHERE tenant_id = ${tenantId} AND ...) or explicitly iterate per-tenant with validated tenant IDs. If the intention is a global admin scan, make that explicit and ensure results are handled in a tenant-isolated way (or persisted separately per tenant).
- **server/firecrawl.ts:184** — UPDATE on scrapedPages uses only id in WHERE (eq(scrapedPages.id, existing[0].id)) and does not include an explicit tenant filter.
  - _Fix:_ Constrain the UPDATE to the caller's tenant as well. e.g. .where(and(eq(scrapedPages.id, existing[0].id), eq(scrapedPages.tenantId, tenantId))). Always include tenantId in UPDATE WHERE clauses on tenant-scoped tables.
- **server/firecrawl.ts:307** — UPDATE on scrapedPages (inside crawl loop) uses only id in WHERE (eq(scrapedPages.id, existing[0].id)) and lacks an explicit tenant filter.
  - _Fix:_ Add the tenant check to the WHERE clause: .where(and(eq(scrapedPages.id, existing[0].id), eq(scrapedPages.tenantId, tenantId))). Ensure all UPDATEs on tenant-scoped tables include tenant scoping.
- **server/firecrawl.ts:386** — Use of sql`...` with a request-derived value (options.search) inside the WHERE clause: sql`(${scrapedPages.content} ILIKE ${'%' + options.search + '%'} OR ${scrapedPages.title} ILIKE ${'%' + options.search + '%'})`. This mixes request data into a raw SQL template, risking injection + isolation issues.
  - _Fix:_ Avoid constructing raw SQL with request-derived strings. Use Drizzle's parameterized predicates (e.g. ilike(scrapedPages.content, `%${options.search}%`) and ilike(scrapedPages.title, `%${options.search}%`)) or bind the search term as a parameter through the query builder instead of embedding it in a sql`` template.
- **server/firecrawl.ts:459** — Use of sql`` with request-derived options.pageIds: sql`${scrapedPages.id} = ANY(${options.pageIds})`. This embeds user input into a raw SQL template.
  - _Fix:_ Use parameterized array/in operator provided by the query builder (e.g. where(inArray(scrapedPages.id, options.pageIds)) or appropriate Drizzle helpers) so the array is passed as bound parameter rather than interpolated into raw SQL.
- **server/google-drive.ts:560** — UPDATE on projects sets driveFolderId/driveFolderUrl with WHERE eq(projects.id, projectId) but does not include an explicit tenant filter, even though this is a tenant-scoped resource.
  - _Fix:_ Include tenant scoping on the UPDATE to prevent cross-tenant updates. For example: .where(and(eq(projects.id, projectId), eq(projects.tenantId, tenantId))). Alternatively, use an UPDATE JOIN or perform the update via the previously-loaded project row with an explicit tenant check.
- **server/google-drive.ts:1009** — Raw SQL (db.execute(sql`...`)) embeds a request-derived pattern into a LIKE clause: AND LOWER(pf.file_name) LIKE ${'%' + pattern + '%'} (pattern comes from params.namePattern). This is using a sql`` template with user input.
  - _Fix:_ Use Drizzle's parameterized query builder or pass the pattern as a bound parameter rather than embedding it in raw SQL. Example: .where(ilike(project_files.file_name, `%${pattern}%`)) or build the sql with explicit bind parameters to avoid interpolating user data directly into raw SQL.
- **server/google-drive.ts:1023** — Second raw SQL branch (db.execute(sql`...`)) similarly embeds a request-derived pattern into LOWER(pf.file_name) LIKE ${'%' + pattern + '%'} and uses tenantId interpolation in a sql`` template.
  - _Fix:_ As above, avoid raw sql`` with request-derived values. Use parameterized Drizzle predicates (ilike) or bind the values via the query builder. Ensure tenant scoping remains present and user input is never interpolated directly into SQL strings.
- **server/health-audit.ts:314** — SELECT from code_proposals lacks tenant filter — query pulls stale proposals across all tenants.
  - _Fix:_ Scope the query to a tenant (e.g. add WHERE tenant_id = ${tenantId}) or require an explicit admin/maintenance context and iterate per-tenant. Alternatively fetch only IDs already filtered to this tenant before acting.
- **server/health-audit.ts:346** — SELECT from heartbeat_tasks lacks tenant filter — query inspects heartbeat tasks globally (may be tenant-scoped).
  - _Fix:_ If heartbeat_tasks are tenant-scoped, add AND tenant_id = ${tenantId} (or run the scanner per-tenant). If this is intentionally global, enforce an explicit admin-only execution path and document why cross-tenant access is required.
- **server/heartbeat.ts:52** — SELECT from tenant-scoped table research_schedules without tenant_id constraint.
  - _Fix:_ Restrict the query to a specific tenant (e.g. add WHERE tenant_id = ${someTenantId}) or iterate per-tenant via a storage helper that enforces tenant scoping. Avoid global SELECT * FROM research_schedules without an explicit tenant filter.
- **server/heartbeat.ts:101** — UPDATE research_schedules SET ... WHERE id = ${sched.id} — no tenant_id constraint on update of tenant-scoped table.
  - _Fix:_ Include tenant filtering in the WHERE clause (e.g. WHERE id = ${sched.id} AND tenant_id = ${sched.tenant_id}) or use a storage-layer method that asserts the tenant context before updating.
- **server/heartbeat.ts:106** — UPDATE research_schedules SET ... WHERE id = ${sched.id} — no tenant_id constraint on update of tenant-scoped table.
  - _Fix:_ Add AND tenant_id = ${sched.tenant_id} to the WHERE clause or call a tenant-aware storage API that validates ownership (e.g. assertTenantContext(task.tenantId) then update with tenant filter).
- **server/heartbeat.ts:112** — UPDATE research_schedules SET ... WHERE id = ${sched.id} — missing tenant_id constraint.
  - _Fix:_ Ensure updates include tenant_id in the WHERE clause (WHERE id = ${sched.id} AND tenant_id = ${sched.tenant_id}) or use storage helpers that enforce tenant scoping.
- **server/heartbeat.ts:120** — UPDATE research_schedules SET next_run_at = ${recoveryNext} WHERE id = ${sched.id} — no tenant_id filter on tenant-scoped table.
  - _Fix:_ Constrain by tenant (e.g. WHERE id = ${sched.id} AND tenant_id = ${sched.tenant_id}) or update via a tenant-aware storage function that verifies ownership.
- **server/heartbeat.ts:248** — UPDATE heartbeat_tasks SET model = ... WHERE id = ${t.id} — writing to tenant-scoped heartbeat_tasks without tenant_id constraint.
  - _Fix:_ Add tenant_id = ${t.tenant_id} to the WHERE clause or use a storage API that enforces the task's tenant (or call assertTenantContext and then update using tenant filter).
- **server/heartbeat.ts:252** — UPDATE heartbeat_tasks SET model = ... WHERE id = ${t.id} — missing tenant_id constraint on tenant-scoped table.
  - _Fix:_ Include tenant scoping in the update (WHERE id = ${t.id} AND tenant_id = ${t.tenant_id}) or perform the update via a tenant-aware storage helper that validates tenant ownership.
- **server/heartbeat.ts:526** — SELECT * FROM event_log WHERE status = 'pending' ... — reading a tenant-scoped event_log without tenant_id constraint.
  - _Fix:_ Process events in a tenant-aware manner: include tenant_id in the WHERE clause for single-tenant processing or iterate per-tenant. Alternatively, use an application-level queue/storage API that returns only events for the tenant being processed and enforces tenant ownership.
- **server/heartbeat.ts:1503** — SELECT created_at FROM heartbeat_logs WHERE task_name = 'Dream Memory Consolidation' ... — querying heartbeat_logs (tenant-scoped) with no tenant filter.
  - _Fix:_ Scope the query by tenant (e.g. WHERE task_name = ... AND status = 'success' AND tenant_id = ${tenantId}) or call a tenant-aware storage function that enforces the tenant context before reading logs.
- **server/heartbeat.ts:2772** — UPDATE heartbeat_tasks SET approval_status = 'pending' WHERE id = ${newTask.id} — writing to tenant-scoped heartbeat_tasks without tenant_id in WHERE.
  - _Fix:_ Constrain the UPDATE to the task's tenant (e.g. WHERE id = ${newTask.id} AND tenant_id = ${tenantId}) or use storage.updateHeartbeatTask/newTask API that applies tenant scope and verifies ownership.
- **server/heartbeat.ts:3130** — UPDATE heartbeat_tasks SET approval_status = 'pending' WHERE id = ${newTask.id} — missing tenant_id constraint on tenant-scoped table.
  - _Fix:_ Include tenant_id in the WHERE clause (WHERE id = ${newTask.id} AND tenant_id = ${tenantId}) or perform the update via a tenant-aware storage helper that asserts the tenant context.
- **server/internal-resolver.ts:91** — UPDATE event_log is executed without constraining by tenant_id. This allows the resolver to finalize (or mutate) event rows belonging to other tenants.
  - _Fix:_ Require and pass the caller's tenantId into finalizeEvent and add AND tenant_id = ${tenantId} (or use drizzle eq(event_log.tenantId, tenantId)) in the WHERE clause. Validate ownership before mutating shared rows; if this is intended to be a cross-tenant system operation, make that explicit and gate it to a privileged system/admin path.
- **server/internal-resolver.ts:106** — SELECT FROM plans WHERE id = ${planId} is executed with no tenant filter. The code reads a tenant-scoped plans row by id only, which can return rows for other tenants.
  - _Fix:_ Load the plan by both id AND tenant_id (e.g. WHERE id = ${planId} AND tenant_id = ${tenantId}), or otherwise verify the plan belongs to the caller's tenant before taking actions. Ensure the resolver is passed an explicit tenant context for authorization.
- **server/internal-resolver.ts:170** — UPDATE code_proposals flips status for a proposal id without restricting by tenant_id. This can archive/alter proposals across tenants.
  - _Fix:_ Add AND tenant_id = ${tenantId} to the UPDATE WHERE clause (and any RETURNING/reading) or verify ownership explicitly before updating. Ensure tenant is supplied to the resolver and used in all queries against tenant-scoped tables.
- **server/job-queue.ts:95** — claimDueJobs issues an UPDATE ... WHERE id IN (SELECT id FROM agent_jobs ...) without constraining by tenant_id. This atomically claims jobs across all tenants.
  - _Fix:_ If workers are intended to operate per-tenant, pass tenantId into claimDueJobs and include tenant_id = ${tenantId} in the inner SELECT/outer UPDATE. If the worker must be global, require an explicit privileged/system caller and document/gate cross-tenant behavior; otherwise add tenant checks to enforce isolation.
- **server/job-queue.ts:131** — reclaimExpiredLeases performs an UPDATE on agent_jobs to mark rows failed_terminal without any tenant_id constraint, affecting jobs from all tenants.
  - _Fix:_ Scope reclaim operations by tenant where appropriate (pass tenantId and include tenant_id = ${tenantId}), or explicitly restrict this code to a privileged system context. Prefer explicit tenancy checks rather than blanket global updates on tenant-scoped tables.
- **server/job-queue.ts:144** — reclaimExpiredLeases re-queues expired jobs with an UPDATE that lacks a tenant_id filter, touching pending/running rows across all tenants.
  - _Fix:_ Include tenant_id = ${tenantId} in the WHERE clause (or otherwise ensure the caller is authorized to act across tenants). Ensure reclaim logic cannot inadvertently affect other tenants' jobs.
- **server/job-queue.ts:173** — completeJob updates agent_jobs SET status='succeeded' WHERE id = ${id} ... without constraining by tenant_id. A caller could complete a job belonging to another tenant if they know the id.
  - _Fix:_ Add tenant scoping to the UPDATE (AND tenant_id = ${tenantId}) and require the caller to provide or validate tenantId as part of the operation. Use lease-fencing plus tenant checks for safer multi-tenant handling.
- **server/job-queue.ts:201** — failJob SELECT max_attempts FROM agent_jobs WHERE id = ${id} has no tenant_id constraint, allowing reads of another tenant's job metadata.
  - _Fix:_ Select max_attempts with tenant scoping (WHERE id = ${id} AND tenant_id = ${tenantId}) or otherwise validate the job belongs to the caller's tenant before using the result.
- **server/job-queue.ts:207** — failJob UPDATE to mark failed_terminal is executed without tenant_id in the WHERE clause, permitting cross-tenant state changes.
  - _Fix:_ Include AND tenant_id = ${tenantId} in the WHERE clause and require the caller to pass tenantId. Enforce tenant ownership checks before marking terminal failures.
- **server/job-queue.ts:223** — failJob UPDATE to reschedule a job (set status='pending') lacks tenant scoping in the WHERE clause and can requeue jobs for other tenants.
  - _Fix:_ Add tenant_id = ${tenantId} to the WHERE clause (or require system-level authorization) so retries/reschedules cannot be performed across tenants inadvertently.
- **server/job-queue.ts:238** — cancelJob updates agent_jobs to cancel a job by id without a tenant_id constraint, allowing cancellation of jobs from other tenants.
  - _Fix:_ Include AND tenant_id = ${tenantId} in the WHERE clause and require the caller to provide/verify tenant ownership before cancelling a job.
- **server/job-queue.ts:248** — retryJob resets job status via UPDATE WHERE id = ${id} without restricting by tenant_id, permitting cross-tenant retries.
  - _Fix:_ Restrict retry operations to the tenant owning the job by adding tenant_id = ${tenantId} in the WHERE clause and validating caller identity/authorization.
- **server/knowledge-diversity-monitor.ts:266** — runKnowledgeDiversityMonitor selects tenant_id, persona_id, COUNT(*) FROM agent_knowledge with no tenant filter and iterates over all tenants' data. This reads across tenant-scoped rows without per-caller tenant checks.
  - _Fix:_ Either (a) run this as a privileged system job (explicitly documented/gated) and ensure it cannot be triggered by unprivileged tenants, or (b) restrict the operation to a single tenant at a time by passing tenantId and adding WHERE tenant_id = ${tenantId}. Never perform ad-hoc scans of tenant-scoped tables from untrusted request contexts.
- **server/lib/agent-trace.ts:124** — persistSpanClose runs UPDATE agent_trace_spans ... WHERE span_id = ${spanId} without tenant_id in the predicate, allowing closing/updating spans belonging to other tenants by span_id alone.
  - _Fix:_ Include tenant scoping in the UPDATE (e.g. AND tenant_id = ${tenantId}) and have the close operation require the tenant from context (or fetch and verify the tenant before updating). Ensure both open and close paths enforce the same tenant ownership checks.
- **server/lib/bwb-job-progress.ts:144** — UPDATE on tenant-scoped table `video_jobs` uses .where(scope(jobId,...)) which may omit the tenant_id predicate if BWB_TENANT_ID is missing. This can lead to cross-tenant updates when the env-threaded tenant is not present.
  - _Fix:_ Require an explicit tenantId for these operations (or fail if env BWB_TENANT_ID is not set). Always include eq(videoJobs.tenantId, tenantId) in the WHERE clause (do not rely on a best-effort env-derived predicate). Consider making scope(...) throw if tenantId is absent so callers cannot silently drop the tenant predicate.
- **server/lib/bwb-job-progress.ts:169** — UPDATE on tenant-scoped table `video_jobs` in updateBwbChapters uses .where(scope(jobId,...)) which may omit tenant_id when the threaded BWB_TENANT_ID is absent, permitting cross-tenant writes.
  - _Fix:_ Require and validate an explicit tenantId argument and ensure the WHERE always contains eq(videoJobs.tenantId, tenantId). Alternatively, make scope() enforce tenant presence (throw/log and abort) rather than silently dropping the tenant predicate.
- **server/lib/bwb-job-progress.ts:203** — SELECT from tenant-scoped table `video_jobs` uses .where(scope(jobId)) where scope may omit tenant_id if the BWB_TENANT_ID env/thread value is missing. This reads rows across tenants.
  - _Fix:_ Verify tenant ownership before reading: include eq(videoJobs.tenantId, tenantId) in the WHERE or require the caller to pass tenantId and validate it. Fail closed if the tenant cannot be established.
- **server/lib/bwb-job-progress.ts:211** — UPDATE on tenant-scoped table `video_jobs` uses .where(scope(jobId)) which may omit tenant_id if the BWB_TENANT_ID is not set, allowing updates to other tenants' rows.
  - _Fix:_ Always include eq(videoJobs.tenantId, tenantId) in the WHERE for updates. Do not rely on an optional env-threaded tenant; require explicit tenantId or make scope() enforce presence of tenantId.
- **server/lib/bwb-job-progress.ts:230** — UPDATE (heartbeat) on tenant-scoped table `video_jobs` uses .where(scope(jobId,...)) where scope can drop the tenant predicate when BWB_TENANT_ID is missing, enabling cross-tenant modification.
  - _Fix:_ Require explicit tenantId for heartbeat calls or modify scope() to always enforce tenant presence. Ensure the WHERE always contains eq(videoJobs.tenantId, tenantId) before performing any update.
- **server/lib/bwb-job-progress.ts:254** — UPDATE in failBwbJob uses .where(scope(jobId, { notDone:true })) and scope(...) may omit tenant_id if BWB_TENANT_ID is absent — this can mark other tenants' jobs as failed.
  - _Fix:_ Ensure tenant scoping is mandatory: accept tenantId explicitly or make scope() throw when tenantId cannot be resolved. Always include eq(videoJobs.tenantId, tenantId) in the WHERE for destructive updates.
- **server/lib/mcp-api-keys.ts:166** — SELECT from tenant-scoped table `mcp_api_keys` without a tenant_id constraint (lookup by key_prefix does not include tenant scoping).
  - _Fix:_ If the design requires resolving tenant from the presented key, make that explicit and defend subsequent writes. Ensure the lookup is globally unique (unique index on key_prefix) and, after resolving the row, always include tenant_id in any follow-up writes. Alternatively, maintain a mapping table (prefix → tenant_id) so lookups remain safe. Concretely: after selecting the row, use its tenant_id for any later queries/updates and/or change the SELECT to an explicitly audited cross-tenant lookup function with tight checks and logging.
- **server/lib/mcp-api-keys.ts:182** — UPDATE mcp_api_keys SET last_used_at = NOW() WHERE id = ${row.id} executes without also constraining by tenant_id.
  - _Fix:_ Include tenant_id in the WHERE clause (e.g. WHERE id = ${row.id} AND tenant_id = ${row.tenant_id}) so writes are always scoped to the resolved tenant. This prevents accidental cross-tenant updates if IDs are ever mis-associated or the lookup changes.
- **server/lib/scheduled-post-runner.ts:164** — UPDATE scheduled_posts ... WHERE id = ${row.id} is executed without also constraining by tenant_id.
  - _Fix:_ Include tenant_id in the WHERE clause (e.g. WHERE id = ${row.id} AND tenant_id = ${row.tenant_id}) to ensure the update cannot affect another tenant's row. Prefer binding both id and tenant_id from the previously-returned row for every post-processing write.
- **server/lib/scheduled-post-runner.ts:191** — Error-handling branch updates scheduled_posts (UPDATE ... WHERE id = ${row.id} AND status = 'publishing') without a tenant_id constraint.
  - _Fix:_ Add tenant_id to the WHERE clause (e.g. WHERE id = ${row.id} AND tenant_id = ${row.tenant_id} AND status = 'publishing') so failure-path writes are also tenant-scoped and cannot modify other tenants' rows.
- **server/lib/stuck-task-sweep.ts:56** — SELECT against tenant-scoped table `agent_runs` executed with no tenant_id restriction (raw SQL WHERE lacks tenant filter). This reads rows across all tenants.
  - _Fix:_ Add an explicit tenant filter (e.g. `AND tenant_id = ${tenantId}`) or require an admin-only audited path that documents and enforces privileged access. Prefer changing the function to accept a tenantId and include it in the WHERE clause, or restrict this query to an authorized admin context only.
- **server/lib/stuck-task-sweep.ts:84** — SELECT against tenant-scoped table `mind_tickets` executed with no tenant_id restriction (raw SQL WHERE lacks tenant filter). This will enumerate rows across tenants.
  - _Fix:_ Add an explicit tenant filter (e.g. `AND tenant_id = ${tenantId}`) or require an admin-only audited path that enforces privileged access. Prefer changing the function to accept a tenantId and include it in the WHERE clause, or document/guard this as an explicit cross-tenant admin audit operation.
- **server/memory-graph.ts:125** — UPDATE of `memory_categories.memory_count` uses a subquery that counts from `memory_entries` but the subquery does not restrict by tenant_id. This will compute counts across tenants and can corrupt per-tenant counts.
  - _Fix:_ Include tenant scoping in the subquery (e.g. `... FROM memory_entries WHERE category_id = ${categoryId} AND tenant_id = ${tenantId} AND status = 'active' ...`). To do that, change refreshCategoryCount to accept a tenantId (or lookup the category's tenant_id first) and use it in the WHERE clause.
- **server/memory-graph.ts:66** — Writing path: `UPDATE memory_categories SET memory_count = memory_count + 1 WHERE id = ${catId}` is executed without constraining by tenant_id in the case where a new top-level category is created. This increments a category row by id only and can affect categories across tenants.
  - _Fix:_ Include tenant scoping in the UPDATE (e.g. `WHERE id = ${catId} AND tenant_id = ${tenantId}`), or retrieve/verify the category's tenant_id and use it in the WHERE. Prefer adding tenantId as an explicit parameter to the code paths that modify memory_categories so all writes include tenant constraints.
- **server/memory-intelligence.ts:273** — UPDATE against tenant-scoped table memory_entries without tenant_id constraint (category_id update).
  - _Fix:_ Include tenant_id in the WHERE clause (e.g. WHERE id = ${entry.id} AND tenant_id = ${tenantId}) or use a storage helper that enforces tenant scoping/ownership before updating.
- **server/memory-intelligence.ts:316** — UPDATE against tenant-scoped table memory_entries without tenant_id constraint (category_id update in update flow).
  - _Fix:_ Same fix: add AND tenant_id = ${tenantId} to the WHERE clause or call a tenant-aware storage API that verifies ownership before updating.
- **server/memory-intelligence.ts:399** — UPDATE of knowledge_triples sets valid_until WHERE id = ${c.id} but omits tenant_id — tenant-scoped table update missing tenant constraint.
  - _Fix:_ Constrain the UPDATE to the caller's tenant, e.g. WHERE id = ${c.id} AND tenant_id = ${tenantId}, so rows from other tenants cannot be modified.
- **server/minerva-planner.ts:373** — SELECT probe (SELECT status, ceo_decision FROM plans WHERE id = ${args.planId}) reads plan row without tenant_id filtering — potential IDOR/cross-tenant read.
  - _Fix:_ Restrict the probe SELECT to the caller's tenant (add AND tenant_id = ${tenantId}) or fetch the row using a tenant-aware helper to guarantee ownership before acting on it.
- **server/minerva-planner.ts:391** — SELECT for child plan (SELECT id FROM plans WHERE parent_plan_id = ${args.planId} ORDER BY id DESC LIMIT 1) lacks tenant_id filter — may return plans from other tenants.
  - _Fix:_ Add tenant scoping to the query (AND parent_plan_id = ${args.planId} AND tenant_id = ${tenantId}) or otherwise ensure the parent-child lookup is restricted to the caller's tenant before trusting results.
- **server/minerva-planner.ts:238** — createPlan defaults tenantId to 1 when args.tenantId is omitted (const tenantId = args.tenantId ?? 1), risking writes into the admin tenant when callers omit tenantId.
  - _Fix:_ Fail closed on a missing tenantId: require tenantId in the function signature and throw if missing, or validate the caller's session and derive tenantId from a trusted source rather than defaulting to tenant 1.
- **server/model-catalog.ts:698** — runCatalogSync defaults tenantId to 1 (const tenantId = opts.tenantId ?? 1) before inserting memory_entries — a missing tenantId will write alerts into tenant 1.
  - _Fix:_ Require and validate tenantId, failing closed if absent. Do not default to tenant 1; obtain tenantId from a trusted/authenticated context or throw an error when the caller omits it.
- **server/mpeg-engine.ts:1132** — INSERT into tenant-scoped table `project_files` does not include tenant_id. Every INSERT into tenant-scoped tables must explicitly set tenant_id (columns are NOT NULL/no default).
  - _Fix:_ Pass and persist the tenant id in the INSERT (e.g. include tenant_id = ${options.tenantId} in the column list/VALUES). You already call assertProjectInTenant — keep that check, but also add tenant_id to the INSERT so the row is stored under the correct tenant.
- **server/mpeg-engine.ts:1487** — INSERT into tenant-scoped table `project_files` does not include tenant_id (parallel path).
  - _Fix:_ Include tenant_id = ${options.tenantId} in the INSERT VALUES and ensure ownership was validated (assertProjectInTenant) before writing. Do not rely on implicit defaults.
- **server/oauth-subscriptions.ts:934** — UPDATE on `oauth_subscriptions` uses WHERE id = ${sub.id} without tenant_id constraint — mutates a tenant-scoped row by id only, lacking an explicit tenant_id guard.
  - _Fix:_ Include tenant_id in the WHERE clause (e.g. WHERE id = ${sub.id} AND tenant_id = ${tenantId}) or otherwise ensure the statement is executed only within an already-validated per-tenant context/transaction. Avoid updating tenant-scoped rows by id alone without tenant scoping.
- **server/oauth-subscriptions.ts:944** — Another UPDATE on `oauth_subscriptions` uses WHERE id = ${sub.id} without tenant_id constraint (deactivation path).
  - _Fix:_ Add tenant_id to the WHERE clause (WHERE id = ${sub.id} AND tenant_id = ${tenantId}) or otherwise enforce and assert the tenant context before performing the update to prevent cross-tenant writes.
- **server/oauth-subscriptions.ts:1236** — proactiveTokenRefresh selects all active rows from `oauth_subscriptions` (WHERE is_active = TRUE) without tenant_id filtering — this iterates over subscriptions for all tenants.
  - _Fix:_ Do per-tenant refresh iterations (SELECT ... WHERE tenant_id = ${tenantId}) or restrict this operation to a dedicated admin process with explicit authorization. Never perform unscoped reads over tenant-scoped secrets/tokens in ordinary request paths.
- **server/oauth-subscriptions.ts:1306** — forceRefreshAllSubscriptions SELECTs provider, tenant_id FROM `oauth_subscriptions` WHERE is_active = TRUE with no tenant constraint (global scan of tenant-scoped table).
  - _Fix:_ If this operation must touch all tenants, ensure it runs in a hardened admin-only context and/or change to scanning an explicit tenant list and performing per-tenant parameterized queries. Otherwise scope to tenant_id for normal runtime paths.
- **server/orchestrator-ledger.ts:444** — Use of sql.raw() with ledger lock key: tx.execute(sql`SELECT pg_advisory_xact_lock(${sql.raw(lockKey)}::bigint)`). lockKey is derived from caller-supplied ledgerKey and tenantId — sql.raw() inserts raw SQL text and can lead to injection or isolation issues when given request-derived values.
  - _Fix:_ Avoid sql.raw() for values derived from untrusted input. Compute a numeric BigInt and pass it as a parameterized value (e.g. sql`SELECT pg_advisory_xact_lock(${BigInt(lockKey)})`) or otherwise ensure proper parameterization/validation. Validate and tightly constrain ledgerKey input before use.
- **server/orchestrator-ledger.ts:452** — UPDATE memory_entries SET ... WHERE id = ${row.id} updates a tenant-scoped table using only id (no tenant_id filter). Even though id came from a prior SELECT, the UPDATE lacks an explicit tenant_id constraint in the WHERE clause.
  - _Fix:_ Include tenant_id in the UPDATE WHERE clause (e.g. WHERE id = ${row.id} AND tenant_id = ${tenantId}) or bind the tenant into the transaction context and assert it before mutating. Prefer explicit tenant scoping on all UPDATE/DELETE statements to prevent accidental cross-tenant modifications.
- **server/pace-control.ts:53** — Query against heartbeat_logs lacks a tenant_id constraint — SELECT COUNT(*) ... FROM heartbeat_logs WHERE created_at > ... (no tenant filter). This allows cross-tenant counting and enforces caps globally instead of per-tenant.
  - _Fix:_ Make pace checks tenant-scoped. Pass the caller's tenantId into checkPace and restrict the query (e.g. WHERE tenant_id = ${tenantId} AND created_at > ...). If heartbeat_logs is intentionally global, document and revisit design; otherwise add tenant_id column filter or use admin-only codepath.
- **server/pace-control.ts:103** — getPaceSnapshot performs queries against heartbeat_logs without constraining by tenant (SELECT COUNT / GROUP BY persona_name FROM heartbeat_logs WHERE created_at > ...). This will return/aggregate data across all tenants.
  - _Fix:_ Accept a tenantId parameter and add WHERE tenant_id = ${tenantId} to these queries (or otherwise limit results to the intended tenant). Ensure any dashboard/diagnostic endpoints calling this are authorized for multi-tenant reads if cross-tenant aggregation is required.
- **server/personality-files.ts:8** — CREATE TABLE personality_files declares tenant_id INTEGER NOT NULL DEFAULT 1. The DEFAULT allows inserts that omit tenant_id, violating the invariant that every INSERT must explicitly pass tenant_id and preventing strict application-level enforcement of tenant ownership.
  - _Fix:_ Remove the DEFAULT on tenant_id and make tenant_id simply NOT NULL (no default). Migrate existing rows explicitly (if needed) and update all callers to always pass tenantId on INSERTs. Consider adding a check constraint or runtime guard to fail when tenant_id is omitted.
- **server/plan-executor.ts:336** — Code updates plan status to 'failed' when a plan has no steps using UPDATE plans SET status = 'failed' WHERE id = ${planId} — no tenant_id filter. This can change other tenants' plan rows.
  - _Fix:_ Add tenant constraint to the update (e.g. WHERE id = ${planId} AND tenant_id = ${tenantId}). Ensure planId is always validated against the caller's tenant before performing status transitions.
- **server/plan-executor.ts:457** — Error-path recovery UPDATE sets status = 'failed' WHERE id = ${planId} AND status = 'executing' but omits tenant_id. This can flip another tenant's plan to failed if planId is misused.
  - _Fix:_ Include tenant_id = ${tenantId} in the WHERE clause and derive tenantId from the claimed/validated plan row before running this update.
- **server/plan-executor.ts:473** — Boot-time recovery UPDATE resets all stale 'executing' plans to 'approved' without tenant scoping: UPDATE plans SET status = 'approved' WHERE status = 'executing' AND updated_at < ... RETURNING id. This operates across all tenants.
  - _Fix:_ If boot recovery must operate across tenants, make sure the caller has admin authority and document this exception. Otherwise add tenant scoping (e.g. only reset for the current tenant) or perform per-tenant controlled recovery. Consider limiting the scope or requiring an admin-only path with explicit authorization.
- **server/plan-executor.ts:482** — resumeStuckPlans selects pending plans via SELECT id FROM plans WHERE status = 'approved' ORDER BY id ASC LIMIT 50 with no tenant restriction; it will enqueue plans across tenants for execution.
  - _Fix:_ Either add a tenant filter and run recovery per-tenant, or ensure this function is admin-only and explicitly intended to operate cluster-wide. If it should be tenant-scoped, accept tenantId and use WHERE tenant_id = ${tenantId} AND status = 'approved'.
- **server/proactive-engine.ts:138** — SELECT persona_id FROM proactive_actions WHERE id = ${actionId} reads a row from a tenant-scoped table without constraining by tenant_id — caller can learn/act on a row belonging to another tenant.
  - _Fix:_ Include tenant scoping on the SELECT (e.g. WHERE id = ${actionId} AND tenant_id = ${tenantId}) or first verify that the actionId belongs to the caller's tenant before reading/using the row.
- **server/process-governor.ts:121** — Subqueries counting heartbeat_logs use task_id but do not include tenant_id (SELECT COUNT(*) FROM heartbeat_logs WHERE task_id = ht.id ...). heartbeat_logs is tenant-scoped but the subquery lacks tenant scoping, risking cross-tenant reads.
  - _Fix:_ Add tenant filtering to the subqueries (e.g. AND tenant_id = ht.tenant_id or AND tenant_id = ${tenantId}) so counts are constrained to the caller's tenant.
- **server/process-governor.ts:155** — Pending-tasks subquery uses heartbeat_tasks WHERE persona_id = ad.persona_id without tenant constraint. heartbeat_tasks is tenant-scoped; this can return counts across tenants.
  - _Fix:_ Constrain the subselect by tenant (for example: WHERE persona_id = ad.persona_id AND tenant_id = ad.tenant_id) or otherwise join on tenant_id = ${tenantId} to ensure results are scoped to the caller's tenant.
- **server/process-governor.ts:199** — SELECT AVG(duration_ms) FROM heartbeat_logs ... lacks tenant_id filter. This reads tenant-scoped performance data globally rather than per-tenant.
  - _Fix:_ Add AND tenant_id = ${tenantId} (or equivalent) to the WHERE clause so the aggregation is limited to the caller's tenant.
- **server/process-governor.ts:240** — SELECT COUNT(DISTINCT persona_id) FROM heartbeat_logs ... has no tenant_id condition — reading tenant-scoped heartbeat_logs across tenants.
  - _Fix:_ Add tenant scoping (e.g. AND tenant_id = ${tenantId}) to this query to restrict counts to the caller's tenant.
- **server/process-governor.ts:595** — UPDATE heartbeat_tasks SET enabled = false WHERE id = ${t.id} performs a write to a tenant-scoped table using only id; it does not include tenant_id in the WHERE clause.
  - _Fix:_ Include tenant scoping in the UPDATE (e.g. WHERE id = ${t.id} AND tenant_id = ${tenantId}) to ensure the mutation only affects the caller's tenant's row.
- **server/process-governor.ts:641** — UPDATE agent_desks SET ... WHERE id = ${desk.id} updates a tenant-scoped table without constraining by tenant_id.
  - _Fix:_ Add AND tenant_id = ${tenantId} (or derive tenant from ad) to the WHERE clause to prevent cross-tenant modifications.
- **server/project-brain.ts:135** — SELECT name, status, description FROM projects WHERE id = ${projectId} reads a tenant-scoped projects table using only id and no tenant_id constraint — allows cross-tenant project access.
  - _Fix:_ Require and use tenantId in this function and add WHERE tenant_id = ${tenantId} (e.g. WHERE id = ${projectId} AND tenant_id = ${tenantId}) or otherwise validate the project's tenant before reading/using project data.
- **server/project-brain.ts:332** — Backfill query SELECT DISTINCT p.id, p.name FROM projects p JOIN conversations c ON c.project_id = p.id ... does not filter by tenant_id for projects/conversations/messages — can enumerate projects and conversations across tenants.
  - _Fix:_ Add tenant constraints to the query (e.g. WHERE p.tenant_id = ${tenantId} AND c.tenant_id = ${tenantId}) or accept/derive tenantId and ensure every involved table is filtered by that tenant.
- **server/project-brain.ts:349** — SELECT c.id, c.title FROM conversations c WHERE c.project_id = ${proj.id} lacks tenant scoping; conversations are tenant-scoped and this can return rows for other tenants.
  - _Fix:_ Add AND c.tenant_id = ${tenantId} (and require tenantId passed into backfill) so conversation queries are limited to the caller's tenant.
- **server/project-brain.ts:359** — SELECT role, content FROM messages WHERE conversation_id = ${conv.id} reads messages without a tenant filter. messages are tenant-scoped and must be constrained by tenant_id.
  - _Fix:_ Include AND tenant_id = ${tenantId} in the WHERE clause (and propagate tenantId into this flow) to avoid cross-tenant data leakage.
- **server/proposal-verifier.ts:33** — SELECT ... FROM code_proposals WHERE id = ${proposalId} reads a tenant-scoped code_proposals row by id only — no tenant_id constraint. This can expose proposals across tenants.
  - _Fix:_ Require caller tenantId (or verify proposal ownership) and include AND tenant_id = ${tenantId} in the SELECT, or fetch the row and then validate its tenant_id matches the caller before proceeding.
- **server/recurring-messages.ts:252** — SELECT from tenant-scoped table 'agent_knowledge' is executed without a tenant_id constraint (global scan).
  - _Fix:_ Restrict the SELECT to a specific tenant_id (e.g. WHERE tenant_id = ${tenantId}) or require/validate an explicit global-run flag and document the justification. If a global scan is necessary, ensure the call site is explicitly privileged and each row is enforced/validated before any write or sensitive action.
- **server/reference-learner.ts:283** — UPDATE memory_entries SET ... WHERE id = ${storedId} is executed without a tenant_id check. An attacker or bug could cause an update to a memory entry owned by another tenant.
  - _Fix:_ Add AND tenant_id = ${input.tenantId} (or the stored persona owner) to the WHERE clause, or validate storedId ownership before issuing the UPDATE.
- **server/research-engine.ts:156** — UPDATE research_sessions ... WHERE status = 'running' ... (zombie cleanup) runs without any tenant_id constraint and will affect sessions across all tenants.
  - _Fix:_ Limit cleanup to a specific tenant_id when possible, or document+gate this as an explicit cross-tenant administrative operation only callable by privileged code. Prefer parameterizing tenant scope or iterate per-tenant to avoid accidental cross-tenant changes.
- **server/research-engine.ts:309** — UPDATE research_programs SET model = ${fallback} WHERE id = ${programId} does not include tenant_id in the WHERE clause; it can modify another tenant's program if programId is not validated.
  - _Fix:_ Include AND tenant_id = ${tenantId} in the WHERE clause, or resolve and verify the program belongs to the current tenant before performing the UPDATE.
- **server/research-engine.ts:458** — SELECT name, persona_id FROM research_programs WHERE id = ${session.programId} is performed without tenant_id filtering; it may return a program from a different tenant.
  - _Fix:_ Add AND tenant_id = ${session.tenantId} to the WHERE clause, or otherwise validate the program row belongs to the session's tenant before using the data.
- **server/research-engine.ts:545** — UPDATE research_sessions SET ... WHERE id = ${sessionId} (endSession) lacks a tenant_id constraint. Writing session state without validating tenant ownership can permit cross-tenant modifications.
  - _Fix:_ Include tenant_id = ${session.tenantId} in the WHERE clause for the UPDATE, or double-check that the session row belongs to the expected tenant prior to the update.
- **server/quarterly-intelligence.ts:344** — CREATE TABLE model_registry_updates defines tenant_id INTEGER NOT NULL DEFAULT 1. A default tenant_id allows INSERTs that omit tenant_id to be associated with tenant 1, defeating application-level tenant isolation.
  - _Fix:_ Remove the DEFAULT 1 and require tenant_id to be passed explicitly on INSERT. Ensure application code always supplies tenant_id on INSERTs and consider adding defensive checks/CI to catch missing tenant_id columns.
- **server/routes.ts:4404** — SELECT from tenant-scoped table 'conversations' without constraining by tenant_id. The query SELECT project_id FROM conversations WHERE id = ${conversationId} ... lacks AND tenant_id = ${tenantId}, so it can read a conversation belonging to a different tenant.
  - _Fix:_ Constrain the lookup to the caller's tenant and/or validate ownership first. e.g. SELECT project_id FROM conversations WHERE id = ${conversationId} AND tenant_id = ${tenantId} OR perform an application-level ownership check that verifies the conversation belongs to req's tenant before reading the project_id.
- **server/routes.ts:7381** — Calls to storage.createMessage() when starting a template-created conversation do not include tenantId (e.g. createMessage({ conversationId: conv.id, ... })). Messages are tenant-scoped but tenant_id is not passed explicitly on INSERT.
  - _Fix:_ Pass the tenantId explicitly when creating messages. Example: await storage.createMessage({ conversationId: conv.id, role: 'system', content: template.systemPromptPrefix, tenantId }); and likewise when creating starter user messages. If storage.createMessage derives tenantId internally, make that explicit or document/enforce it; prefer passing tenantId to satisfy the NOT NULL tenant column requirement.
- **server/routes.ts:6679** — Import handler persists tenant-scoped records without explicitly setting tenant_id. Examples: storage.createMemoryEntry({ ...rest, personaId: ... }), storage.createKnowledge({ ...rest, personaId: ... }), storage.createMessage({ ...msgRest, conversationId: newConv.id }), storage.createHeartbeatTask(rest) — none of these calls explicitly set tenantId.
  - _Fix:_ When importing, ensure each tenant-scoped INSERT includes tenantId explicitly (e.g. include tenantId from the exported object or the target tenant). For messages/memory/knowledge/heartbeat tasks, call storage APIs with tenantId: await storage.createMessage({ ..., conversationId: newConv.id, tenantId: newConv.tenantId }) or extend the import logic to set tenantId on the created rows and validate ownership before inserting.
- **server/routes/conversations.ts:98** — UPDATE on tenant-scoped table 'conversations' performed without constraining by tenant (no WHERE tenant_id = ...) — UPDATE conversations SET project_id = ${projectId} WHERE id = ${conv.id}
  - _Fix:_ Include tenant scoping in the UPDATE. e.g. UPDATE conversations SET project_id = ${projectId} WHERE id = ${conv.id} AND tenant_id = ${tenantId} (or use storage/update helper that enforces tenant ownership). Ensure tenantId is derived from the authenticated caller or the conversation object and not from untrusted input.
- **server/routes/conversations.ts:99** — SELECT from tenant-scoped table 'project_conversations' without tenant filter — SELECT id FROM project_conversations WHERE project_id = ${projectId} AND conversation_id = ${conv.id}
  - _Fix:_ Add tenant scoping to the SELECT: SELECT id FROM project_conversations WHERE project_id = ${projectId} AND conversation_id = ${conv.id} AND tenant_id = ${tenantId} (or use a storage API that enforces tenant ownership).
- **server/routes/conversations.ts:102** — INSERT into tenant-scoped table 'project_conversations' omits tenant_id column/value — INSERT INTO project_conversations (project_id, conversation_id) VALUES (...)
  - _Fix:_ Include tenant_id in the INSERT values (and derive it from the authenticated tenant): INSERT INTO project_conversations (tenant_id, project_id, conversation_id) VALUES (${tenantId}, ${projectId}, ${conv.id}), or call a storage helper that sets tenant_id automatically after verifying ownership.
- **server/routes/conversations.ts:104** — UPDATE on tenant-scoped table 'projects' performed without tenant constraint — UPDATE projects SET updated_at = CURRENT_TIMESTAMP WHERE id = ${projectId}
  - _Fix:_ Constrain the UPDATE to the tenant: UPDATE projects SET updated_at = CURRENT_TIMESTAMP WHERE id = ${projectId} AND tenant_id = ${tenantId}. Prefer checking ownership first or using storage API that enforces tenant scoping.
- **server/routes/conversations.ts:208** — SELECT from tenant-scoped table 'conversations' performed without tenant filter in DELETE flow — SELECT c.project_id FROM conversations c JOIN projects p ON p.id = c.project_id WHERE c.id = ${id} AND c.project_id IS NOT NULL AND p.tenant_id = ${conv.tenantId ?? tenantId} (missing c.tenant_id)
  - _Fix:_ Add c.tenant_id filter to the WHERE clause (e.g. AND c.tenant_id = ${conv.tenantId ?? tenantId}) to ensure the conversation row read is scoped to the caller's tenant (or use the storage API that enforces tenant ownership).
- **server/routes/conversations.ts:228** — INSERT into tenant-scoped table 'project_notes' omits tenant scoping/tenant_id — INSERT INTO project_notes (project_id, note, author) VALUES (...)
  - _Fix:_ Include tenant_id in the inserted row (and derive it from authenticated context or the project): INSERT INTO project_notes (tenant_id, project_id, note, author) VALUES (${tenantId}, ${projId}, ${note}, ${author}), or call a storage helper that enforces tenant ownership to avoid creating notes under the wrong tenant.
- **server/routes/heartbeat.ts:102** — UPDATE on tenant-scoped table 'heartbeat_tasks' performed by tenant admin check without tenant constraint — UPDATE heartbeat_tasks SET ... WHERE id = ${taskId} (no tenant_id predicate)
  - _Fix:_ Constrain the UPDATE to the tenant owning the task. e.g. determine taskTenantId (from storage.getHeartbeatTask(taskId) or the authenticated tenant) and run: UPDATE heartbeat_tasks SET ... WHERE id = ${taskId} AND tenant_id = ${taskTenantId}. Prefer using storage helper that enforces tenant scoping.
- **server/routes/heartbeat.ts:116** — UPDATE on tenant-scoped table 'heartbeat_tasks' performed without tenant constraint — UPDATE heartbeat_tasks SET approval_status = 'rejected', enabled = false WHERE id = ${taskId}
  - _Fix:_ Add tenant scoping: UPDATE heartbeat_tasks SET ... WHERE id = ${taskId} AND tenant_id = ${taskTenantId} (derive taskTenantId from the task or from authenticated tenant). Use a storage-layer call that enforces tenant ownership if available.
- **server/routes/inbox-notifications.ts:101** — UPDATE on tenant-scoped table 'inbox_messages' executed without tenant constraint when marking a single message read — UPDATE inbox_messages SET is_read = TRUE WHERE id = ${msg.id}
  - _Fix:_ Constrain the UPDATE by tenant: UPDATE inbox_messages SET is_read = TRUE WHERE id = ${msg.id} AND tenant_id = ${tenantId}. Alternatively, use a storage method that enforces tenant ownership before mutating the row.
- **server/routes/stripe-checkout.ts:102** — Raw/string-interpolated SQL (sql`... ${priceId} ...`) is executed with a request-derived value (priceId). This is flagged by the invariant because using string-interpolated/raw SQL with request inputs increases risk of SQL injection and makes it easy to bypass application-level tenant-isolation checks.
  - _Fix:_ Avoid embedding request-derived values inside raw/sql-template queries. Use the Drizzle query builder (db.select().from(...).where(eq(table.id, priceId), ...)) so conditions are expressed via the typed API, and/or pass parameters explicitly rather than interpolating into raw SQL. Also ensure any referenced foreign rows are validated for tenant ownership where appropriate (e.g. if prices become tenant-scoped in future). Concretely, replace the db.execute(sql`... ${priceId} ...`) block with a db.select().from(stripePrices).where(eq(stripePrices.id, priceId), eq(stripePrices.active, true), eq(stripeProducts.active, true)) (joining products via Drizzle), and validate ownership or anonymization rules in application code.
- **server/transactional-snapshot.ts:407** — Rollback uses pool.query to UPDATE action_snapshots WHERE id = $1 with no tenant_id constraint. This can clear undone_at on a snapshot belonging to a different tenant (cross-tenant undo).
  - _Fix:_ Include tenant scoping in the WHERE clause (e.g. WHERE id = $1 AND tenant_id = $2) and pass ctx.tenantId as a parameter, or otherwise verify the row belongs to ctx.tenantId before updating. Prefer using the same db.execute(sql`...`) pattern with ${ctx.tenantId} interpolation or Drizzle .where(eq(table.tenantId, tenantId)).
- **server/sculptor.ts:93** — UPDATE sculptor_sessions SET status = ... WHERE id = ${session.id} is executed without constraining tenant_id. This can modify another tenant's session row if an id collision or tampered id occurs.
  - _Fix:_ Add tenant scoping to the update (e.g. WHERE id = ${session.id} AND tenant_id = ${params.tenantId}) or use a Drizzle .where(eq(sculptorSessions.tenantId, params.tenantId)) to ensure the update only affects the caller's tenant.
- **server/sculptor.ts:100** — UPDATE sculptor_sessions SET conversation_id = ... WHERE id = ${session.id} lacks a tenant_id check. Row updates must be tenant-scoped to prevent cross-tenant writes.
  - _Fix:_ Constrain the UPDATE with tenant_id (e.g. WHERE id = ${session.id} AND tenant_id = ${params.tenantId}) or use Drizzle/parameterized WHERE with the tenantId passed into this function.
- **server/sculptor.ts:156** — monitorSession updates sculptor_sessions (SET status, result, tool_calls_count, tokens_used, completed_at) WHERE id = ${sessionId} without a tenant_id restriction. This allows writes across tenants.
  - _Fix:_ Include tenant scoping in the UPDATE (e.g. WHERE id = ${sessionId} AND tenant_id = ${tenantId}) — pass tenantId into monitorSession and use it in the WHERE or use Drizzle .where with the tenantId equality check.
- **server/sculptor.ts:346** — UPDATE sculptor_sessions SET review_result = ... WHERE id = ${sessionId} is missing a tenant_id constraint. This can write review results into another tenant's session row.
  - _Fix:_ Add tenant scoping to the UPDATE (e.g. WHERE id = ${sessionId} AND tenant_id = ${tenantId}); ensure tenantId is passed into reviewSessionWork and used in all writes to sculptor_sessions (or use Drizzle .where with eq on tenantId).
- **server/seed.ts:34** — Unscoped aggregate SELECT on tenant-scoped table research_sessions: `SELECT COUNT(*) as count FROM research_sessions` has no tenant_id filter.
  - _Fix:_ Restrict the count to the caller tenant (e.g. `WHERE tenant_id = ${tenantId}`) or otherwise pass an explicit tenantId into the query (or use Drizzle .where(eq(research_sessions.tenant_id, tenantId))). Avoid global aggregates across tenants.
- **server/seed.ts:3204** — SELECT from tenant-scoped table 'conversations' is executed without constraining tenant_id. The query SELECT DISTINCT c.id, c.project_id FROM conversations c ... lacks any WHERE c.tenant_id = <tenant> filter, so it can read rows across all tenants.
  - _Fix:_ Add an explicit tenant filter (e.g. AND c.tenant_id = ${tenantId}) to the SELECT and ensure the subordinate existence-check on project_conversations also respects tenant boundaries (or join via project → tenant). If this is intended to run for a specific tenant in seeding, hardcode or parameterize tenant_id; otherwise iterate per-tenant.
- **server/seed.ts:3212** — INSERT into project_conversations uses project_id and conversation_id derived from an earlier cross-tenant SELECT without verifying tenant ownership. The code inserts links based on r.project_id / r.id that may belong to other tenants.
  - _Fix:_ Before inserting links, verify the referenced conversation/project rows belong to the expected tenant (e.g. SELECT ... WHERE id = $id AND tenant_id = $tenantId) and include tenant_id in the inserted row (or otherwise encode tenant context). Ensure you don't create cross-tenant references.
- **server/seed.ts:4402** — UPDATE memory_entries performs a global update (REPLACE) without restricting by tenant_id. memory_entries is tenant-scoped (idx_memory_entries_tenant_id exists), so this will modify memory facts across all tenants.
  - _Fix:_ Restrict the UPDATE to a specific tenant (AND tenant_id = ${tenantId}) or iterate tenants individually. If the intent is a one-off global migration, run it per-tenant or require an explicit admin/maintenance path; never run tenant-scoped mutations without tenant context in application code.
- **server/seed.ts:4030** — A Drizzle select reads heartbeat_tasks without any tenant filter: await db.select().from(heartbeatTasks). This returns heartbeat tasks across all tenants.
  - _Fix:_ Constrain Drizzle selects to the caller's tenant (e.g. .where(eq(heartbeatTasks.tenantId, tenantId))). If a global view is intended, make that explicit and ensure appropriate authorization; otherwise operate per-tenant.
- **server/seed.ts:4373** — DELETE FROM heartbeat_tasks WHERE enabled = false is executed without a tenant constraint. heartbeat_tasks is tenant-scoped (indexes and seed logic show tenant_id usage), so this will delete rows for all tenants.
  - _Fix:_ Add a tenant restriction (DELETE ... WHERE enabled = false AND tenant_id = ${tenantId}) or perform the deletion per-tenant. Avoid mass-deletes across tenants without explicit multi-tenant maintenance authorization.
- **server/seed.ts:4492** — Multiple SELECT/UPDATE statements inside cleanupRunawayDelegationTasks operate on tenant-scoped table 'heartbeat_tasks' without any tenant_id constraints (e.g. SELECT count(*) FROM heartbeat_tasks WHERE enabled = true AND type = 'delegation'; UPDATE heartbeat_tasks SET enabled = false WHERE type = 'delegation' ...; UPDATE cron_expression WHERE name = ...; SELECT/UPDATE name ILIKE ...). These will read/modify heartbeat tasks for all tenants.
  - _Fix:_ Ensure every SELECT/UPDATE/DELETE on heartbeat_tasks includes an explicit tenant filter (e.g. WHERE tenant_id = ${tenantId}). If the function is intended to run globally, make that explicit and gate it behind a dedicated maintenance/admin flow; otherwise iterate per-tenant and validate ownership before mutating rows.
- **server/self-improvement.ts:165** — detectStagnation builds a SELECT on experiments that may lack a tenant filter when tenantId is not supplied. If tenantId is omitted the query can return experiments from all tenants.
  - _Fix:_ Require a tenantId parameter (or default to the caller's tenant) and always include eq(experiments.tenantId, tenantId) in the WHERE clause. Do not issue an unscoped select against experiments.
- **server/self-improvement.ts:511** — getExperimentHistory returns db.select().from(experiments) with no tenant filter when tenantId is not provided — this will expose experiments across tenants.
  - _Fix:_ Require caller to pass tenantId (or infer caller's tenant) and always add eq(experiments.tenantId, tenantId) to the WHERE clause. Avoid returning unscoped experiment rows.
- **server/self-improvement.ts:258** — Persona is loaded via storage.getPersona(config.personaId) with no verification that the persona belongs to the caller's tenant, then storage.updatePersona(persona.id, ...) is invoked to mutate the persona. This can result in cross-tenant writes to persona data if storage.* does not enforce tenant ownership.
  - _Fix:_ Before updating a persona, verify tenant ownership (e.g. fetch persona with tenant context or check persona.tenantId === config.tenantId). Require and pass tenantId to storage.getPersona/updatePersona, or otherwise enforce an ownership check prior to any persona mutation.
- **server/skill-seeker.ts:300** — detectGap signature defaults tenantId to 1 and then INSERTs capability_gaps using that tenant_id. If callers omit tenantId this will create rows under the admin tenant (tenant 1), causing cross-tenant writes and misattribution.
  - _Fix:_ Do not default tenantId to 1. Require an explicit tenantId (or infer the caller's tenant) and validate it. Ensure INSERT uses the caller's tenant and fail-closed if tenant context is missing.
- **server/skill-seeker.ts:743** — listGaps has branches that execute SELECT * FROM capability_gaps without a tenant_id filter (e.g. when only status is provided, or when neither status nor tenantId is provided), exposing rows from all tenants.
  - _Fix:_ Enforce tenant scoping on capability_gaps queries. Require tenantId or restrict results to the caller's tenant; never run an unscoped SELECT on tenant-scoped tables. Add WHERE tenant_id = ${tenantId} to all SELECT/UPDATE/DELETE against capability_gaps unless an explicit admin-only code path is used with proper authorization.
- **server/skillify.ts:196** — SELECT from tenant-scoped table `conversations` is performed without constraining by tenant_id in the WHERE clause.
  - _Fix:_ Include tenant scoping in the query (e.g. .where(and(eq(conversations.id, conversationId), eq(conversations.tenantId, tenantId))) or validate tenant via an assert helper before/within the query (use tenantScope / assertTenantContext or an assertConversationInTenant helper). Failing-closed checks should be applied at the SQL-level rather than relying only on post-query checks.
- **server/skillify.ts:223** — SELECT from `personas` by persona id is executed without verifying the persona belongs to the caller's tenant (no tenant_id constraint). This reads a foreign id (personaId) derived from a conversation row without validating ownership.
  - _Fix:_ Query personas with tenant filtering (e.g. .where(and(eq(personas.id, conv[0].personaId), eq(personas.tenantId, conv[0].tenantId) )) or explicitly assert the persona's tenant matches the caller (fail-closed). Use existing tenant-guard helpers or add assertConversationInTenant/assertPersonaInTenant logic prior to the read.
- **server/storage.ts:241** — getConversationUnscoped performs an unscoped SELECT on tenant-scoped table `conversations` (WHERE id = ${id}) with no tenant_id constraint.
  - _Fix:_ Avoid unscoped reads in storage layer. Require a tenantId parameter or explicitly require callers to pass/validate tenant ownership before calling. If an unscoped accessor is unavoidable, make it private/internal and add detailed audit/logging + lint gating; prefer returning tenantId along with the row and validating upstream before exposing data.
- **server/storage.ts:245** — createConversation inserts into tenant-scoped table `conversations` using values(data) without guaranteeing data.tenantId is set/validated.
  - _Fix:_ Require/validate tenantId at the API boundary and assert it here before INSERT. Explicitly set tenantId in the .values(...) call (e.g. .values({ ...data, tenantId: assertValidTenantId(data.tenantId) })). Reject/throw if tenantId is missing or invalid.
- **server/storage.ts:285** — attachCitationMetadata issues a raw SELECT FROM messages (WHERE id IN (...)) without scoping by tenant_id — reading tenant-scoped data without a tenant filter.
  - _Fix:_ Include tenant scoping in the query (e.g. AND tenant_id = ${tenantId}) or join against conversations to ensure the message ids belong to the expected tenant before returning citations. Require caller to provide and validate tenantId, or fetch citations by (id, tenant_id) pairs.
- **server/storage.ts:386** — attachCitations performs UPDATE messages SET citations = ... WHERE id = ${messageId} with no tenant_id constraint — writes a tenant-scoped table by id only.
  - _Fix:_ Verify the message belongs to the caller's tenant before updating (e.g. UPDATE ... WHERE id = ${messageId} AND tenant_id = ${tenantId}), or load+assert ownership first and then update with a tenant-gated WHERE clause.
- **server/storage.ts:513** — createMemoryEntry inserts into tenant-scoped table `memory_entries` without validating/ensuring data.tenantId is present.
  - _Fix:_ Require tenantId on createMemoryEntry (assertValidTenantId(data.tenantId)) and fail-closed if missing. Explicitly pass tenantId in the INSERT values rather than trusting caller-supplied payload shape.
- **server/storage.ts:731** — updateMemoryEmbedding runs UPDATE memory_entries SET embedding WHERE id = ${id} with no tenant_id constraint.
  - _Fix:_ Include tenant scoping on the UPDATE (e.g. .where(and(eq(memory_entries.id, id), eq(memory_entries.tenantId, tenantId)))) or require the caller to pass and validate tenantId before the update to ensure embeddings are only modified within the owning tenant.
- **server/storage.ts:743** — updateKnowledgeEmbedding runs UPDATE agent_knowledge SET embedding WHERE id = ${id} with no tenant_id constraint.
  - _Fix:_ Require and validate tenantId and add it to the WHERE clause (e.g. AND agent_knowledge.tenant_id = ${tenantId}) or otherwise ensure the caller's tenant owns the knowledge row before mutating it.
- **server/storage.ts:531** — touchMemoryEntries updates memory_entries WHERE id IN (ids) without tenant_id constraint — touching rows across tenants is possible if ids are supplied externally.
  - _Fix:_ Require caller-provided tenantId and include eq(memory_entries.tenantId, tenantId) in the WHERE. Alternatively, resolve ownership for each id before updating and only touch ids owned by the caller's tenant.
- **server/storage.ts:499** — getAllMemoriesForBackup selects ALL rows from tenant-scoped table `memory_entries` with no tenant filter.
  - _Fix:_ If this is an admin/export function, make it explicit and restrict access. For normal code paths, require tenantId and apply tenant scoping. Avoid providing an unauthenticated/undifferentiated API that returns all tenants' memories.
- **server/storage.ts:806** — getMemoryStats can execute unscoped SELECTs on `memory_entries` and `agent_knowledge` when tenantId is undefined (it falls back to selecting all rows), returning cross-tenant data.
  - _Fix:_ Fail-closed: require tenantId for statistical reads or ensure all queries include tenant scoping. If admin-only, clearly label and gate the function; otherwise add assertValidTenantId and use tenantScope in queries.
- **server/storage.ts:711** — createKnowledge INSERTs into tenant-scoped table `agent_knowledge` without validating/ensuring data.tenantId is present.
  - _Fix:_ Require tenantId and validate it (assertValidTenantId) before inserting. Explicitly set tenantId in the INSERT values and fail-closed if missing or invalid.
- **server/storage.ts:752** — getMemoriesWithoutEmbeddings selects active memory_entries without any tenant scoping — returns rows across tenants.
  - _Fix:_ Add an optional tenantId parameter and include tenant scoping in the WHERE clause, or restrict function to admin contexts and gate access appropriately.
- **server/storage.ts:758** — getKnowledgeWithoutEmbeddings selects agent_knowledge rows without tenant scoping (no tenant filter present).
  - _Fix:_ Require tenantId or apply tenantScope(agentKnowledge.tenantId, tenantId) to restrict results to the calling tenant. If intended as global maintenance, mark/admin-gate it explicitly.
- **server/surprise-scorer.ts:93** — stampArgsEmbedding issues UPDATE felix_proposals SET args_embedding = ... WHERE id = ${proposalId} AND args_embedding IS NULL with NO tenant_id constraint.
  - _Fix:_ Include tenant scoping in the UPDATE (AND tenant_id = ${tenantId}) or accept tenantId as a parameter and validate caller ownership before mutating the row to prevent cross-tenant mutation.
- **server/surprise-scorer.ts:139** — scoreProposalSurprise performs UPDATE felix_proposals SET actual_outcome_embedding = ... WHERE id = ${proposalId} with no tenant_id constraint.
  - _Fix:_ Add AND tenant_id = ${tenantId} to the UPDATE or otherwise ensure the row belongs to the expected tenant before mutating. Require tenantId be passed and validated at mutation points.
- **server/surprise-scorer.ts:181** — When not enough neighbors the code runs UPDATE felix_proposals SET surprise_band = 'no_history' WHERE id = ${proposalId} with no tenant filtering.
  - _Fix:_ Scope the UPDATE by tenant_id (e.g. WHERE id = ${proposalId} AND tenant_id = ${tenantId}), or require/propagate tenantId into this path so you never write tenant-scoped columns without tenant validation.
- **server/surprise-scorer.ts:201** — Neighbor-average query uses WHERE id = ANY(${neighborIds}::int[]) and then SELECT AVG(...) FROM felix_proposals WHERE id = ANY(...) with no tenant scoping — AVG runs across IDs without also verifying tenant ownership in the WHERE clause.
  - _Fix:_ Ensure neighborIds are filtered to the same tenant before running the aggregate, or include tenant_id = ${tenantId} in the aggregate's WHERE clause. Prefer parameterizing and verifying ownership upstream rather than relying on id-only predicates.
- **server/surprise-scorer.ts:212** — Final UPDATE writing surprise_score/surprise_band uses WHERE id = ${proposalId} with no tenant_id constraint.
  - _Fix:_ Add tenant filtering (AND tenant_id = ${tenantId}) or require tenantId at this call site; fail-closed if tenantId is missing. Ensure all writes to felix_proposals include tenant scoping.
- **server/tool-learning.ts:129** — SELECT from tenant-scoped table `custom_tools` without constraining by tenant_id (reads not scoped to caller tenant).
  - _Fix:_ Scope the lookup to the caller tenant. For example, add a tenant predicate: .where(and(eq(customTools.name, parsed.name), eq(customTools.tenantId, tenantId))). Prefer using the tenant-scoped unique name + tenantId pair or otherwise validate ownership before trusting results.
- **server/tool-learning.ts:350** — UPDATE on tenant-scoped table `custom_tools` increments usageCount WHERE id = tool.id without constraining by tenant_id — write not scoped to tenant.
  - _Fix:_ Add tenant scoping to the update: .where(and(eq(customTools.id, tool.id), eq(customTools.tenantId, tenantId))). Prefer selecting the row with both id+tenantId earlier and using that canonical verification before any update.
- **server/tools.ts:7351** — INSERT into project_conversations omits tenant_id. Tenant-scoped tables must receive tenant_id explicitly on INSERT to prevent cross-tenant rows.
  - _Fix:_ Include tenant_id in the INSERT values (and/or use an application helper that injects/validates tenantId): INSERT INTO project_conversations (project_id, conversation_id, tenant_id) VALUES (..., ..., ${tenantId}) ON CONFLICT DO NOTHING. Alternatively, validate ownership and set tenant_id from the owning project before inserting.
- **server/tools.ts:7454** — INSERT into project_files does not set tenant_id. Per schema rules tenant-scoped tables must receive tenant_id explicitly on INSERT (columns are NOT NULL with no default).
  - _Fix:_ Include tenant_id in the INSERT: INSERT INTO project_files (project_id, file_name, file_path, file_url, file_type, file_size, uploaded_by, tenant_id) VALUES (..., ${tenantId}); or derive and validate tenantId from the owning project and persist it.
- **server/tools.ts:7468** — INSERT into project_notes omits tenant_id. Writing to a tenant-scoped table without tenant_id can create rows not tied to the caller's tenant or allow cross-tenant contamination.
  - _Fix:_ Add tenant_id to the INSERT (INSERT INTO project_notes (project_id, note, author, tenant_id) VALUES (..., ${tenantId})), or validate the project ownership and set tenant_id accordingly before insert.
- **server/tools.ts:9211** — INSERT into project_files during write_file auto-linking omits tenant_id. This writes a tenant-scoped table row without explicitly setting tenant_id.
  - _Fix:_ Persist tenant_id when inserting project_files here as well (e.g. INSERT INTO project_files (..., tenant_id) VALUES (..., ${tenantId})). Ensure tenantId is derived from the linked project (validated earlier) rather than trusting caller-supplied values.
- **server/tools.ts:10451** — SELECT from tenant-scoped table `memory_entries` lacks tenant filter. The query 'SELECT fact FROM memory_entries WHERE persona_id = 2 AND source = 'slides_qa' AND category = 'lesson' ...' can return other tenants' memory rows because it does not constrain by tenant_id.
  - _Fix:_ Require and use the caller's tenant context, and parameterize the query to filter by tenant_id. Example: ensure params._tenantId is present and change the query to include `AND tenant_id = ${params._tenantId}` (or use the Drizzle .where(eq(table.tenant_id, tenantId)) helper). Alternatively call a tenant-assertion helper before querying to ensure the conversation/tenant ownership.
- **server/tools.ts:14214** — INSERT into project_files does not include tenant_id. The code relies on assertProjectInTenant(...) to verify ownership but still inserts a row without explicitly setting tenant_id on a tenant-scoped table (project_files). With no DB-level RLS / FK cascade, omitting tenant_id violates the invariant that every INSERT into tenant-scoped tables must pass tenantId explicitly (columns are NOT NULL with no default).
  - _Fix:_ Include the tenant_id value explicitly in the INSERT. For example: INSERT INTO project_files (tenant_id, project_id, file_name, file_path, file_url, file_type, file_size, uploaded_by) VALUES (${params._tenantId}, ${params.project_id}, ...). Or use the project's tenant value fetched by assertProjectInTenant and pass it into the INSERT via parameterized query / Drizzle insert so every row carries the correct tenant_id.
- **server/tools.ts:15454** — INSERT into project_files does not include tenant_id. Similar to the generate_audio branch, code checks assertProjectInTenant(...) but then inserts into project_files without an explicit tenant_id column/value. If project_files is tenant-scoped (tenant_id NOT NULL), this breaks the rule that every INSERT must set tenant_id explicitly and risks cross-tenant data consistency issues.
  - _Fix:_ Add tenant_id to the INSERT columns and values. E.g. INSERT INTO project_files (tenant_id, project_id, file_name, file_path, file_url, file_type, file_size, uploaded_by) VALUES (${params._tenantId}, ${params.project_id}, ...). Prefer using parameterized Drizzle inserts or include the authoritative tenantId obtained from assertProjectInTenant to ensure correct per-tenant attribution.
- **server/tools.ts:20579** — getOrCreateTenantInbox is invoked with tenantId taken from params (tenantId || 1) inside a fire-and-forget task — this can create/read tenant-scoped inboxes for arbitrary tenant ids provided by the caller (and silently default to admin tenant 1 for missing context).
  - _Fix:_ Avoid creating/reading tenant-scoped inboxes using untrusted tenant ids. Use the authenticated tenant from server context, and do NOT default to tenant 1 for unscoped calls unless an explicit, auditable system actor path is used. Validate that the caller owns the tenant id before calling getOrCreateTenantInbox, or restrict this code path to system-only executions.
- **server/venture-discovery/budget.ts:155** — UPDATE of agent_cost_ledger in settleVentureReservation executes WHERE id = ${reservationId} with no tenant_id constraint. This lets a caller (or bug) update a reservation row belonging to another tenant by id.
  - _Fix:_ Ensure updates to tenant-scoped agent_cost_ledger are tenant-scoped. Either accept/require tenantId and include AND tenant_id = ${tenantId} in the WHERE clause, or lookup+verify the reservation row belongs to the expected tenant before performing the UPDATE. Apply the same guard when settling reservations returned by reserveVentureBudget.
- **server/venture-discovery/budget.ts:170** — UPDATE of agent_cost_ledger in releaseVentureReservation executes WHERE id = ${reservationId} with no tenant_id constraint. This can zero out reservations for other tenants if reservationId is misused.
  - _Fix:_ Scope the UPDATE to the tenant: require tenantId (or fetch the reservation row first and verify its tenant_id) and include AND tenant_id = ${tenantId} in the WHERE clause. Fail closed if tenant mismatch.
- **server/video-job-runner.ts:228** — recoverStaleVideoJobs updates videoJobs rows across all tenants (no tenant_id filter) to mark stale jobs failed. This is an unscoped UPDATE on a tenant-scoped table and can affect jobs for any tenant.
  - _Fix:_ Either run recovery per-tenant (iterate known tenant ids and scope the UPDATE by tenant_id), or require an explicit admin/system context for cross-tenant maintenance and document/limit that path. If the intent is per-tenant scoping, include AND tenant_id = ${tenantId} in the WHERE clause; otherwise ensure this maintenance runs under an auditable privileged operation and not as a normal tenant-scoped codepath.
- **server/webhook-triggers.ts:60** — Query joins webhook_triggers to the personas table (tenant-scoped) with no tenant_id constraint on personas — LEFT JOIN personas p ON p.id = w.persona_id will return persona rows from any tenant.
  - _Fix:_ Constrain persona lookups to the expected tenant (e.g. ADMIN_TENANT_ID) or validate persona ownership before returning/joining. For example: LEFT JOIN personas p ON p.id = w.persona_id AND p.tenant_id = ${ADMIN_TENANT_ID} — or fetch the persona in a separate query and assert p.tenant_id matches the admin tenant before exposing persona_name.
- **server/webhook-triggers.ts:122** — processTriggerEvent selects webhook trigger rows and left-joins personas without enforcing a tenant filter on the personas table. This can expose persona data from other tenants or allow use of a persona that belongs to a different tenant.
  - _Fix:_ When loading trigger.persona_id, validate that the referenced persona belongs to the intended tenant (e.g. ADMIN_TENANT_ID) before using it. Either add a tenant constraint to the JOIN (AND p.tenant_id = ${ADMIN_TENANT_ID}) or perform a separate persona lookup that checks tenant ownership and rejects/cleans mismatches.
- **server/webhook-triggers.ts:81** — createTrigger inserts a personaId provided by the caller into webhook_triggers.persona_id without verifying that the persona belongs to the admin (or otherwise expected) tenant. This lets an attacker register a trigger referencing a persona from another tenant.
  - _Fix:_ Validate personaId before insert: fetch the persona and assert its tenant_id matches ADMIN_TENANT_ID (or the trigger owner's tenant). Reject or clear personaId when the check fails. Alternatively, disallow arbitrary personaId in the public API and require server-side selection of admin personas only.
- **server/webhook-triggers.ts:167** — getOrCreateTriggerConversation uses trigger.persona_id directly when creating a conversation in the ADMIN_TENANT_ID context without validating that the persona belongs to the admin tenant. This can cause cross-tenant persona usage/leakage.
  - _Fix:_ Before calling storage.createConversation with personaId, verify the persona's tenant ownership (e.g. storage.getPersona(personaId, expectedTenant) or SELECT persona.tenant_id WHERE id = personaId and confirm it equals ADMIN_TENANT_ID). If the persona does not belong to the expected tenant, clear or replace it with an admin-owned persona.
- **server/wiring-invariants.ts:244** — checkDormantTools issues COUNT queries against tool_performance and messages (SELECT count(*) FROM tool_performance / SELECT count(*) FROM messages) without filtering by tenant_id, exposing/aggregating per-tenant activity across all tenants.
  - _Fix:_ Add tenant filtering (WHERE tenant_id = :tenantId) or perform the check per-tenant. If you intend a global metric, run under an explicit admin routine that documents the cross-tenant nature and uses safe admin credentials; otherwise avoid global scans of tenant-scoped tables.
- **server/wiring-invariants.ts:293** — checkDormantTools SELECT DISTINCT tool_name FROM tool_performance (recent window) does not filter by tenant_id, so tools/usage are aggregated across tenants.
  - _Fix:_ Filter by tenant_id (e.g. WHERE tenant_id = :tenantId AND last_success_at > ...) or compute per-tenant usage. Avoid blanket scans of tenant-scoped tables without tenant constraints.
- **server/wiring-invariants.ts:361** — checkSubsystemFreshness uses sql.raw(...) and SELECT COUNT/MAX FROM ${spec.table} (dynamic table name) without any tenant_id constraint. This queries tenant-scoped subsystem tables (e.g., knowledge_nudges, tool_performance) across all tenants and also uses raw string interpolation for table access.
  - _Fix:_ Avoid raw string-building for table access where possible; use Drizzle helpers or well-audited whitelists and, crucially, add tenant filtering (e.g. SELECT COUNT(*) FROM ${spec.table} WHERE tenant_id = :tenantId). If you truly need a cross-tenant admin probe, centralize and document it as an explicit admin-only operation and protect it accordingly.
- **server/wiring-invariants.ts:417** — checkToolDrift queries tool_performance (aggregations, GROUP BY) without constraining by tenant_id, so fail/success counts and failure timestamps are aggregated across all tenants.
  - _Fix:_ Add a tenant_id filter (WHERE tenant_id = :tenantId) or run the drift check per-tenant. If an aggregated global view is required, implement it under a clearly labeled admin routine with strict access control and review whether cross-tenant aggregation is acceptable.
- **server/wiring-invariants.ts:535** — checkMemoryGrowth uses sql.raw to SELECT COUNT(*) and pg_total_relation_size FROM memory tables (agent_knowledge, memory_entries, knowledge_triples, graph_memory, messages, compaction_archives) without tenant filtering. This inspects tenant-scoped memory tables across all tenants.
  - _Fix:_ Perform counts per-tenant (add WHERE tenant_id = :tenantId or iterate tenants) or, if you must measure global table size, separate the storage-level size metric (pg_total_relation_size) from per-tenant row counts and document the global nature. Prefer parameterized/Drizzle APIs instead of raw interpolation for row counts.
- **server/wiring-invariants.ts:561** — Later in checkMemoryGrowth the code does SELECT COUNT(*)::int AS cnt FROM graph_memory without any tenant_id constraint, and the log message explicitly asserts "across all tenants." This reads tenant-scoped data globally without enforcement in the application layer.
  - _Fix:_ If graph_memory is tenant-scoped, restrict the check to a tenant (WHERE tenant_id = :tenantId) or iterate tenants. If graph_memory is intentionally global/shared, document that and move it to a schema/table that is clearly global; otherwise add per-tenant safeguards and ensure queries include tenant_id.

### MEDIUM (39)

- **server/agentic/autonomous-closer.ts:161** — runAutonomousCodeCloser defaults tenantId to 1 when opts.tenantId is not provided (tenantId = opts.tenantId ?? 1). Defaulting to owner tenant can cause accidental cross-tenant actions if callers forget to pass tenantId.
  - _Fix:_ Remove the implicit default; require callers to provide tenantId and validate it at runtime (throw if missing/invalid). If a default is necessary, make it explicit and documented and audit all callers to ensure it's correct.
- **server/agentic/harness-adaptation.ts:71** — SELECT ... FROM agent_trace_spans ... WHERE status IN (...) AND metadata->>'modelId' IS NOT NULL ... — query against (likely) tenant-scoped agent_trace_spans with no tenant_id filter.
  - _Fix:_ Restrict the query to a specific tenant (add WHERE tenant_id = ${tenantId}) or explicitly document/guard that this nightly job runs in a global/admin context. If global scanning is required, ensure it only runs under a tightly-controlled admin process/role and add auditing or per-tenant aggregation that avoids exposing tenant-specific trace rows.
- **server/auto-memorize.ts:47** — SELECT from agent_knowledge to read watermark (SELECT content FROM agent_knowledge WHERE category = 'auto_memorize_watermark' AND title = ${WATERMARK_TITLE}) contains no tenant_id constraint. Watermark writes use tenant_id=1 but reads are unscoped.
  - _Fix:_ Either scope watermark reads to the tenant you intend (e.g. tenant_id = ${tenantId}) or make the global watermark access explicit and protect it behind a platform-admin guard; document why this is intentionally global if so.
- **server/auto-memorize.ts:62** — SELECT id FROM agent_knowledge WHERE category = 'auto_memorize_watermark' AND title = ${WATERMARK_TITLE} LIMIT 1 lacks tenant scoping. Subsequent UPDATE uses the id without tenant check.
  - _Fix:_ Include tenant_id in the selection and subsequent UPDATE/DELETE/INSERT operations, or require an explicit admin-only global-watermark flow.
- **server/auto-consolidation.ts:335** — Scheduler scans memory_entries for active tenants (SELECT DISTINCT tenant_id FROM memory_entries WHERE status = 'active' AND tenant_id IS NOT NULL) without limiting to a caller tenant. This enumerates tenant IDs from a tenant-scoped table.
  - _Fix:_ This enumerating query is expected for a scheduler, but make the admin intent explicit: ensure the code is only runnable by an authorized background process or platform-admin, and log/audit access. If not intended, restrict to tenant-scoped operations.
- **server/autonomy.ts:185** — createRule inserts persona_id from caller input directly into the tenant-scoped autonomy_rules table without validating that the referenced persona belongs to the same tenant.
  - _Fix:_ Before inserting, validate that personaId (if provided) exists and is owned by the tenant (e.g. SELECT id FROM personas WHERE id = $1 AND tenant_id = $2). Reject or clear personaId if the ownership check fails, or enforce this with DB constraints (tenant-aware foreign keys or RLS) so rules cannot be associated with personas from another tenant.
- **server/autonomy.ts:211** — updateRule allows changing persona_id (and writes it into autonomy_rules) using caller-provided personaId without verifying that the persona belongs to the tenant.
  - _Fix:_ When accepting a personaId in updates, first verify the persona exists and tenant_id matches the requester (SELECT ... WHERE id = personaId AND tenant_id = tenantId). If validation fails, refuse the update. Alternatively, restrict updates to persona_id to admins or perform server-side mapping that enforces tenant ownership.
- **server/autonomy.ts:160** — logDecision inserts the caller-provided persona_id into the tenant-scoped autonomy_log table without verifying the persona belongs to the tenant.
  - _Fix:_ Validate persona ownership before logging (e.g. SELECT id FROM personas WHERE id = $1 AND tenant_id = $2), or sanitize/omit persona_id when it cannot be validated. Consider adding application-side checks or DB-level protections to prevent cross-tenant persona IDs in logs.
- **server/chat-engine.ts:571** — SELECT from tenant-scoped table `project_files` lacks an explicit tenant_id constraint (WHERE tenant_id = ${tenantId}). The code relies on prior verification of the parent project, but the invariant requires every tenant-scoped table access to include an explicit tenant filter.
  - _Fix:_ Include an explicit tenant filter on the query (e.g. WHERE project_id = ${projectId} AND tenant_id = ${tenantId}) or JOIN the projects table and assert p.tenant_id = ${tenantId}. Alternatively, use a Drizzle .where(eq(projectFiles.tenantId, tenantId)) so every read is explicitly scoped to the caller's tenant.
- **server/chat-engine.ts:582** — SELECT from tenant-scoped table `project_notes` uses only project_id and does not include an explicit tenant_id constraint.
  - _Fix:_ Add tenant scoping to the query (e.g. WHERE project_id = ${projectId} AND tenant_id = ${tenantId}) or otherwise enforce tenant ownership with an explicit WHERE clause or Drizzle .where(eq(projectNotes.tenantId, tenantId)). Do not rely solely on earlier project validation.
- **server/chat-engine.ts:631** — SELECT from tenant-scoped table `compaction_archives` is filtered only by conversation_id and lacks an explicit tenant_id constraint. Even though the conversation was retrieved from a tenant-scoped query earlier, this query does not include tenant scoping itself.
  - _Fix:_ Add tenant filtering to the query (e.g. WHERE conversation_id = ${c.id} AND tenant_id = ${tenantId}) or join to the conversations table with an explicit tenant_id = ${tenantId} check. Prefer using Drizzle .where(eq(compactionArchives.tenantId, tenantId)).
- **server/chat-engine.ts:649** — SELECT from tenant-scoped table `messages` is filtered only by conversation_id and does not include an explicit tenant_id constraint.
  - _Fix:_ Constrain message reads to the tenant (e.g. WHERE conversation_id = ${c.id} AND tenant_id = ${tenantId}) or use Drizzle .where(eq(messages.tenantId, tenantId)). Avoid relying on upstream validation of the conversation alone — every tenant-scoped table access must include an explicit tenant constraint.
- **server/chat-engine.ts:830** — Inline subquery SELECT COUNT(*) FROM project_files WHERE project_id = p.id does not include tenant scoping. The outer query filters projects by p.tenant_id, but the subquery itself lacks an explicit tenant_id constraint on project_files.
  - _Fix:_ Change the subquery to include tenant scoping, e.g. (SELECT COUNT(*) FROM project_files WHERE project_id = p.id AND tenant_id = p.tenant_id) as file_count, or rewrite as a JOIN that enforces tenant_id = ${tenantId}. Use Drizzle-style .where(eq(projectFiles.tenantId, tenantId)) to make tenant ownership explicit.
- **server/data-protection.ts:312** — Reading project_notes by project_id (SELECT title, content FROM project_notes WHERE project_id = ${proj.id}) omits tenant_id predicate — absent explicit tenant scoping on a tenant-scoped table.
  - _Fix:_ Constrain reads to the tenant (e.g. WHERE project_id = ${proj.id} AND tenant_id = ${tenantId}) or ensure project_id was derived from a tenant-scoped query and still include tenant predicate for defense-in-depth.
- **server/data-protection.ts:322** — Reading project_files by project_id (SELECT file_name,... FROM project_files WHERE project_id = ${proj.id}) omits tenant constraint — potential cross-tenant read.
  - _Fix:_ Add AND tenant_id = ${tenantId} to the WHERE clause when selecting project_files, or perform this work inside a tenant-aware transaction to enforce isolation.
- **server/data-protection.ts:356** — COUNT(*) FROM messages WHERE conversation_id = ${conv.id} lacks tenant scoping when used in backupTenantDataToDrive; reads message counts without explicit tenant_id filter.
  - _Fix:_ Include tenant predicate (AND tenant_id = ${tenantId}) in the COUNT query or otherwise verify ownership of conversation IDs before querying tenant-scoped tables.
- **server/doc-collections.ts:166** — DELETE FROM doc_chunks WHERE doc_path = ${docPath} AND collection_id = ${collectionId} has NO tenant_id constraint — deletion operates on a tenant-scoped table without explicit tenant scoping (even though collection was validated earlier).
  - _Fix:_ Add tenant scoping to the DELETE (AND tenant_id = ${tenantId}) to enforce the invariant even when collection_id was pre-validated. Prefer defensive WHERE clauses that include tenant_id rather than relying solely on earlier validation.
- **server/email.ts:350** — SELECT id FROM inbox_messages WHERE message_id = ${msgId} LIMIT 1 runs with NO tenant_id constraint — lookup in a tenant-scoped table is done by message_id only.
  - _Fix:_ If message_id is globally unique and the intent is to dedupe globally, document and enforce that uniqueness at the DB/schema level; otherwise include tenant scoping (e.g. AND tenant_id = ${tenantId}) when checking for existing rows. Prefer explicit tenant filters to avoid accidental cross-tenant data access.
- **server/email.ts:393** — backfillEmptyBodies SELECT id, message_id FROM inbox_messages WHERE (body_text IS NULL OR ...) ORDER BY id executes with NO tenant_id constraint — scanning tenant-scoped inbox_messages across tenants.
  - _Fix:_ Perform backfill per-tenant (pass tenantId) or include tenant_id in the WHERE. If a global scan is required, run it as an offline admin job with explicit privilege and logging rather than in the regular runtime path.
- **server/firecrawl.ts:467** — Use of sql`` with a computed 'days' value derived from request data: sql`${scrapedPages.scrapedAt} < NOW() - make_interval(days => ${days})`. This embeds a request-derived value in raw SQL template.
  - _Fix:_ Compute the cutoff timestamp in application code (e.g. new Date(Date.now() - days*24*60*60*1000)) and pass that Date as a bound parameter in the WHERE clause (e.g. .where(scrapedPages.scrapedAt, '<', cutoffDate)). Avoid injecting request-derived numbers into raw SQL templates.
- **server/google-workspace.ts:2358** — INSERT INTO presenter_slide_images uses session_id and image data without an explicit tenant scope in the INSERT/ON CONFLICT clause. If presenter_slide_images is tenant-scoped, the INSERT/UPSERT lacks tenant_id.
  - _Fix:_ If presenter_slide_images is tenant-scoped, include tenant_id in the INSERT and ON CONFLICT constraint or otherwise constrain/upsert by tenant. Alternatively verify session ownership and/or JOIN to a tenant-scoped parent when performing the write to ensure the row is tied to the caller's tenant.
- **server/google-workspace.ts:2378** — UPDATE presenter_sessions SET slides = ... WHERE id = ${sessionId} executes without an explicit tenant filter. If presenter_sessions is tenant-scoped, this update can modify another tenant's session if sessionId is guessed or abused.
  - _Fix:_ Add tenant scoping to the WHERE clause (e.g. WHERE id = ${sessionId} AND tenant_id = ${tenantId}) or verify session ownership before performing the UPDATE. Always include the tenant_id constraint on UPDATEs to tenant-scoped tables.
- **server/job-queue.ts:347** — getJobStats runs an aggregate SELECT over agent_jobs for the last 7 days without tenant filtering, returning aggregate counts across all tenants.
  - _Fix:_ If stats are intended to be tenant-scoped, add WHERE tenant_id = ${tenantId} (or accept an optional tenantId filter). If global stats are intended, gate the endpoint to admins and document the cross-tenant semantics explicitly.
- **server/lib/martech-bundle.ts:314** — UPDATE on tenant-scoped table `tenant_voice_profiles` uses only id in the WHERE clause (`.where(eq(tenantVoiceProfiles.id, existing.id))`) and does not include tenant_id. Without an ownership constraint this UPDATE could affect a row belonging to a different tenant under some race conditions.
  - _Fix:_ Constrain the UPDATE by tenant as well, e.g. `.where(and(eq(tenantVoiceProfiles.id, existing.id), eq(tenantVoiceProfiles.tenantId, opts.tenantId)))`. Alternatively, fetch-and-verify ownership immediately before update and include tenantId in the WHERE to make the write safe against race/ID-reuse scenarios.
- **server/memory-intelligence.ts:438** — Call to storage.updateMemoryEntry(remove.id, ...) omits tenantId — update may run without verifying tenant ownership.
  - _Fix:_ Pass tenantId into storage.updateMemoryEntry (or otherwise ensure the storage helper enforces tenant_id in its SQL) so the update includes tenant scoping and cannot affect other tenants' rows.
- **server/oauth-subscriptions.ts:893** — SELECT tenant_id,is_active FROM `oauth_subscriptions` WHERE provider = 'google-workspace' has no tenant filter — enumerates rows across tenants.
  - _Fix:_ If you need to discover tenants, do so via an explicitly authorized admin flow or use a separate, purpose-built admin endpoint/backfill that documents cross-tenant intent. Otherwise add WHERE tenant_id = ${tenantId} to scope the query to one tenant.
- **server/routes.ts:2212** — POST /api/presenter inserts presenter_sessions with tenant_id taken directly from req.body (INSERT ... VALUES (${tenantId}, ...)). The handler does not validate that the caller is authorized for the supplied tenantId — tenantId comes from untrusted input.
  - _Fix:_ Do not accept tenantId directly from the request unless the caller is strongly authenticated/authorized for that tenant. Derive tenantId from the caller's authenticated session or validate that the caller (or calling internal service) is allowed to create sessions for the supplied tenantId before inserting. If this endpoint must accept arbitrary tenantId (internal service), restrict it to a dedicated, auditable admin-internal API and add ownership/authorization checks.
- **server/skill-seeker.ts:833** — setupGapDetectionHook's message:sent handler calls detectGap(..., tenantId) where tenantId is derived from event data (data?.tenantId). If tenantId is missing the handler will call detectGap which defaults to tenant 1 — leading to accidental writes into the admin tenant.
  - _Fix:_ Validate that event data contains a valid tenantId before calling detectGap. Do not call detectGap without a verified tenant context; propagate and require tenantId from the event or skip. Remove any reliance on detectGap's default tenant fallback.
- **server/storage.ts:862** — getHeartbeatTask selects from tenant-scoped table `heartbeat_tasks` by id only (WHERE id = ${id}) with no tenant scoping.
  - _Fix:_ Require caller-provided tenantId and include eq(heartbeat_tasks.tenantId, tenantId) in the WHERE clause, or otherwise assert/verify ownership before returning the task.
- **server/storage.ts:914** — getDueHeartbeatTasks selects due heartbeat tasks across all tenants (no tenant filter) — this yields tenant-crossing results.
  - _Fix:_ If scheduler design deliberately processes tasks across tenants, ensure the worker is trusted to operate across tenants. Otherwise require tenant scoping for this query or document and gate the global operation as an explicit admin/worker function.
- **server/storage.ts:929** — claimHeartbeatTasks updates heartbeat_tasks rows (UPDATE ... WHERE id = ${id} AND enabled = true AND next_run_at <= NOW()) without tenant_id constraint — claims could affect tasks across tenants.
  - _Fix:_ Add tenant filtering to the UPDATE (e.g. AND tenant_id = ${tenantId}) or ensure the caller supplies and is authorized for the tenant whose tasks are being claimed. For multi-tenant workers that legitimately claim across tenants, add explicit admin boundaries and audit logging.
- **server/tool-learning.ts:155** — SELECT by id from tenant-scoped table `custom_tools` without verifying tenant_id — reading a tenant row by PK with no tenant constraint.
  - _Fix:_ Include the tenant filter when selecting by id: .where(and(eq(customTools.id, inserted.id), eq(customTools.tenantId, tenantId))). Alternatively, carry the returned row from the original insert (which already contains tenantId) instead of re-querying.
- **server/tools.ts:7447** — SELECT from project_files used to check duplicates lacks an explicit tenant constraint (no tenant_id = <caller tenant> in WHERE). Relying only on project_id without tenant scoping may allow cross-tenant leakage depending on schema assumptions.
  - _Fix:_ Add tenant scoping to the SELECT, e.g. SELECT id FROM project_files WHERE project_id = ${params.id} AND tenant_id = ${tenantId} AND file_name = ${fname} ...; or validate the project belongs to the tenant and include tenant_id in the query to be explicit.
- **server/tools.ts:7488** — SELECT id FROM project_conversations WHERE project_id = ${params.id} AND conversation_id = ${params.conversationId} is missing an explicit tenant_id constraint. Even if project/conversation ownership was checked earlier, the query itself lacks the tenant filter.
  - _Fix:_ Include tenant scoping in the query (AND tenant_id = ${tenantId}) or select/join via the projects table with p.tenant_id = ${tenantId} to ensure the row belongs to the caller's tenant.
- **server/tools.ts:20545** — Calling storage.getTenant(tenantId) with tenantId derived directly from params (params._tenantId || params.tenantId) without verifying the caller's authority over that tenant id.
  - _Fix:_ Do not trust tenantId passed in params. Use the authenticated/request-scoped tenant identity (server-side context) or explicitly verify the caller is authorized for the requested tenant before reading tenant-scoped data. If reading arbitrary tenant metadata is required, restrict this to privileged/system callers and document/guard that path.
- **server/tools.ts:20412** — enforceToolPolicy is called with tenantId taken directly from params (typeof params._tenantId === 'number' ? params._tenantId : 1) — allowing a caller to supply/override tenant context used during policy checks (and defaulting to tenant 1 if missing).
  - _Fix:_ Do not derive policy-check tenant context from untrusted params. Use the authenticated tenant id, and avoid defaulting to admin tenant implicitly. Validate that the tenantId supplied (if any) matches the caller's tenant or is permitted for the actor performing the request.
- **server/video-job-runner.ts:204** — SELECT of cancelRequested from videoJobs in isCancelRequested is performed WHERE jobId = ${jobId} with no tenant_id constraint. Reading a tenant-scoped row without tenant filtering violates the app's tenant-isolation invariant and may allow cross-tenant probing.
  - _Fix:_ Add tenant scoping to the read (e.g. accept tenantId and add AND tenant_id = ${tenantId}), or first fetch the row by jobId then verify row.tenantId === callerTenantId before returning the flag. Prefer explicit tenant-scoped queries rather than jobId-only selects.
- **server/twilio.ts:87** — SELECT from agent_knowledge to find messaging_pairing is executed without a tenant_id filter. agent_knowledge is tenant-scoped, so an unscoped read allows returning pairing records from other tenants.
  - _Fix:_ Scope the pairing lookup to the tenant context if available (e.g. store/require tenant_id on the pairing key and include WHERE tenant_id = ${tenantId}). If the design intentionally allows cross-tenant search, explicitly verify and enforce ownership before using the pairing (and document why cross-tenant scan is needed). Avoid returning or using unscoped agent_knowledge rows.
- **server/twilio.ts:102** — SELECT tenant_id FROM conversations WHERE id = ${info.conversationId} is done without scoping by tenant_id. This reads a tenant-scoped conversations row by id with no tenant filter (the code compares the returned tenant to the pairing afterward, but the initial unscoped read still violates the 'always include tenant filter' invariant).
  - _Fix:_ Query the conversation row with both id and the expected tenant (WHERE id = ${info.conversationId} AND tenant_id = ${info.tenantId}) or fetch by id and immediately verify the tenant matches, but prefer including tenant_id in the WHERE clause so the DB enforces scoping and reduces race/leak surface.

### LOW (2)

- **server/autonomy.ts:285** — seedDefaultRules inserts default rules using hardcoded personaId values (e.g. 2, 11, 15) without verifying those persona ids exist for the tenant, potentially creating rules that reference personas from a different tenant or do not exist.
  - _Fix:_ When seeding defaults, verify that each personaId exists and is owned by the tenant before inserting. If a persona doesn't exist, either skip that rule, insert with persona_id=NULL (global rule), or create tenant-local personas as needed. Add checks to make seeding idempotent and tenant-aware.
- **server/tools.ts:20464** — trackHttpRequest is invoked with tenantId = params._tenantId || 0, using an unvalidated tenant id from caller input when recording network diagnostics/telemetry.
  - _Fix:_ Ensure telemetry/tracking calls use the authenticated tenant identity rather than arbitrary values supplied in params. If accepting a tenant id parameter is necessary, validate ownership or use a sentinel (0) only for system-level calls where no tenant context is intended.
